akr 0.1
project lege-ecosystem

record lege-ecosystem.decode-optimization/1 : work {
    title "Import lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
    state completed
    intent """
        Disposition every durable claim of lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md (docs/12-migration.md).
        """
    acceptance {
        check add-jp2decodestats-claim {
            statement """
                "3. Add `Jp2DecodeStats`" is dispositioned: promoted as lege-ecosystem.work.add-jp2decodestats or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check add-region-of-interest-decode-only-after-claim {
            statement """
                "32. Add region-of-interest decode only after resolution reduction" is dispositioned: promoted as lege-ecosystem.work.add-region-of-interest-decode-only-after or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check approach-a-strided-vertical-kernels-claim {
            statement """
                "Approach A — Strided vertical kernels" is dispositioned: promoted as lege-ecosystem.work.approach-a-strided-vertical-kernels or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check approach-b-tiled-transpose-claim {
            statement """
                "Approach B — Tiled transpose" is dispositioned: promoted as lege-ecosystem.work.approach-b-tiled-transpose or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check approach-c-column-gather-into-reusable-lines-claim {
            statement """
                "Approach C — Column gather into reusable lines" is dispositioned: promoted as lege-ecosystem.work.approach-c-column-gather-into-reusable-lines or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check component-level-parallelism-claim {
            statement """
                "26. Component-level parallelism" is dispositioned: promoted as lege-ecosystem.work.component-level-parallelism or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check core-recommendation-claim {
            statement """
                "39. Core recommendation" is dispositioned: promoted as lege-ecosystem.work.core-recommendation or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check current-decoder-pipeline-claim {
            statement """
                "2. Current decoder pipeline" is dispositioned: promoted as lege-ecosystem.work.current-decoder-pipeline or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check dwt-row-and-column-parallelism-claim {
            statement """
                "27. DWT row and column parallelism" is dispositioned: promoted as lege-ecosystem.work.dwt-row-and-column-parallelism or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check eliminate-temporary-full-image-allocation-for-claim {
            statement """
                "9. Eliminate temporary full-image allocation for single-tile images" is dispositioned: promoted as lege-ecosystem.work.eliminate-temporary-full-image-allocation-for or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check fixed-point-9-7-is-a-later-experiment-claim {
            statement """
                "19. Fixed-point 9/7 is a later experiment" is dispositioned: promoted as lege-ecosystem.work.fixed-point-9-7-is-a-later-experiment or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check full-resolution-correctness-claim {
            statement """
                "34. Full-resolution correctness" is dispositioned: promoted as lege-ecosystem.work.full-resolution-correctness or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check full-resolution-path-claim {
            statement """
                "Full-resolution path" is dispositioned: promoted as lege-ecosystem.work.full-resolution-path or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check fuse-operations-where-useful-claim {
            statement """
                "18. Fuse operations where useful" is dispositioned: promoted as lege-ecosystem.work.fuse-operations-where-useful or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check fuse-tier-1-output-and-dequantization-claim {
            statement """
                "11. Fuse Tier-1 output and dequantization" is dispositioned: promoted as lege-ecosystem.work.fuse-tier-1-output-and-dequantization or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check horizontal-lifting-kernels-claim {
            statement """
                "15. Horizontal lifting kernels" is dispositioned: promoted as lege-ecosystem.work.horizontal-lifting-kernels or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check initial-target-results-claim {
            statement """
                "37. Initial target results" is dispositioned: promoted as lege-ecosystem.work.initial-target-results or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check jp2lam-jpeg-2000-decoder-optimization-plan-claim {
            statement """
                "jp2lam JPEG 2000 Decoder Optimization Plan" is dispositioned: promoted as lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check level-local-operation-claim {
            statement """
                "17. Level-local operation" is dispositioned: promoted as lege-ecosystem.work.level-local-operation or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check material-needed-by-the-implementing-agent-claim {
            statement """
                "38. Material needed by the implementing agent" is dispositioned: promoted as lege-ecosystem.work.material-needed-by-the-implementing-agent or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check milestone-1-decoder-attribution-claim {
            statement """
                "Milestone 1 — Decoder attribution" is dispositioned: promoted as lege-ecosystem.work.milestone-1-decoder-attribution or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check milestone-2-remove-obvious-duplication-claim {
            statement """
                "Milestone 2 — Remove obvious duplication" is dispositioned: promoted as lege-ecosystem.work.milestone-2-remove-obvious-duplication or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check milestone-3-fused-tier-1-output-claim {
            statement """
                "Milestone 3 — Fused Tier-1 output" is dispositioned: promoted as lege-ecosystem.work.milestone-3-fused-tier-1-output or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check milestone-4-reduced-resolution-decode-claim {
            statement """
                "Milestone 4 — Reduced-resolution decode" is dispositioned: promoted as lege-ecosystem.work.milestone-4-reduced-resolution-decode or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check milestone-5-9-7-kernel-optimization-claim {
            statement """
                "Milestone 5 — 9/7 kernel optimization" is dispositioned: promoted as lege-ecosystem.work.milestone-5-9-7-kernel-optimization or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check milestone-6-tier-1-mq-optimization-claim {
            statement """
                "Milestone 6 — Tier-1/MQ optimization" is dispositioned: promoted as lege-ecosystem.work.milestone-6-tier-1-mq-optimization or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check milestone-7-packed-renderer-output-claim {
            statement """
                "Milestone 7 — Packed renderer output" is dispositioned: promoted as lege-ecosystem.work.milestone-7-packed-renderer-output or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check milestone-8-roi-decode-claim {
            statement """
                "Milestone 8 — ROI decode" is dispositioned: promoted as lege-ecosystem.work.milestone-8-roi-decode or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check objective-claim {
            statement """
                "1. Objective" is dispositioned: promoted as lege-ecosystem.work.objective or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check optimize-flaggrid-claim {
            statement """
                "23. Optimize `FlagGrid`" is dispositioned: promoted as lege-ecosystem.work.optimize-flaggrid or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check optimize-mqdecoder-only-after-direct-attribution-claim {
            statement """
                "24. Optimize `MqDecoder` only after direct attribution" is dispositioned: promoted as lege-ecosystem.work.optimize-mqdecoder-only-after-direct-attribution or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check performance-scoreboard-claim {
            statement """
                "36. Performance scoreboard" is dispositioned: promoted as lege-ecosystem.work.performance-scoreboard or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check permanent-decoder-corpus-claim {
            statement """
                "33. Permanent decoder corpus" is dispositioned: promoted as lege-ecosystem.work.permanent-decoder-corpus or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check produce-packed-output-directly-claim {
            statement """
                "12. Produce packed output directly" is dispositioned: promoted as lege-ecosystem.work.produce-packed-output-directly or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check reduced-reconstruction-geometry-claim {
            statement """
                "8. Reduced reconstruction geometry" is dispositioned: promoted as lege-ecosystem.requirement.reduced-reconstruction-geometry or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check reduced-resolution-correctness-claim {
            statement """
                "35. Reduced-resolution correctness" is dispositioned: promoted as lege-ecosystem.work.reduced-resolution-correctness or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check remove-redundant-state-and-traversals-claim {
            statement """
                "21. Remove redundant state and traversals" is dispositioned: promoted as lege-ecosystem.work.remove-redundant-state-and-traversals or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check renderer-integration-policy-claim {
            statement """
                "28. Renderer integration policy" is dispositioned: promoted as lege-ecosystem.policy.renderer-integration-policy or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check replace-eager-packet-position-sorting-where-claim {
            statement """
                "29. Replace eager packet-position sorting where worthwhile" is dispositioned: promoted as lege-ecosystem.policy.replace-eager-packet-position-sorting-where or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check replace-per-code-block-result-allocation-claim {
            statement """
                "10. Replace per-code-block result allocation" is dispositioned: promoted as lege-ecosystem.work.replace-per-code-block-result-allocation or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check replace-the-merged-block-hashmap-claim {
            statement """
                "30. Replace the merged-block `HashMap`" is dispositioned: promoted as lege-ecosystem.work.replace-the-merged-block-hashmap or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check replace-the-single-fixed-output-entry-point-claim {
            statement """
                "5. Replace the single fixed-output entry point internally" is dispositioned: promoted as lege-ecosystem.work.replace-the-single-fixed-output-entry-point or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check required-profiles-claim {
            statement """
                "4. Required profiles" is dispositioned: promoted as lege-ecosystem.work.required-profiles or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check resolution-aware-renderer-path-claim {
            statement """
                "Resolution-aware renderer path" is dispositioned: promoted as lege-ecosystem.work.resolution-aware-renderer-path or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check resolution-selection-in-the-pdf-renderer-claim {
            statement """
                "6. Resolution selection in the PDF renderer" is dispositioned: promoted as lege-ecosystem.work.resolution-selection-in-the-pdf-renderer or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check reusable-bounded-scratch-claim {
            statement """
                "13. Reusable bounded scratch" is dispositioned: promoted as lege-ecosystem.work.reusable-bounded-scratch or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check reuse-per-packet-contribution-storage-claim {
            statement """
                "31. Reuse per-packet contribution storage" is dispositioned: promoted as lege-ecosystem.policy.reuse-per-packet-contribution-storage or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check specialize-the-common-code-block-style-claim {
            statement """
                "20. Specialize the common code-block style" is dispositioned: promoted as lege-ecosystem.work.specialize-the-common-code-block-style or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check tier-1-code-block-parallelism-claim {
            statement """
                "25. Tier-1 code-block parallelism" is dispositioned: promoted as lege-ecosystem.work.tier-1-code-block-parallelism or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check tier-2-behavior-claim {
            statement """
                "7. Tier-2 behavior" is dispositioned: promoted as lege-ecosystem.work.tier-2-behavior or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check treat-9-7-as-the-first-arithmetic-kernel-target-claim {
            statement """
                "14. Treat 9/7 as the first arithmetic kernel target" is dispositioned: promoted as lege-ecosystem.work.treat-9-7-as-the-first-arithmetic-kernel-target or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check use-a-stripe-oriented-scratch-layout-claim {
            statement """
                "22. Use a stripe-oriented scratch layout" is dispositioned: promoted as lege-ecosystem.work.use-a-stripe-oriented-scratch-layout or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check vertical-lifting-layout-claim {
            statement """
                "16. Vertical lifting layout" is dispositioned: promoted as lege-ecosystem.work.vertical-lifting-layout or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
    }
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
    }
}

record lege-ecosystem.decode-optimization-audit/1 : work {
    title "Import lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md"
    state completed
    intent """
        Disposition every durable claim of lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md (docs/12-migration.md).
        """
    acceptance {
        check do-not-start-with-claim {
            statement """
                "Do not start with" is dispositioned: promoted as lege-ecosystem.work.do-not-start-with or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check next-audit-phases-after-structural-dwt-openjpeg-claim {
            statement """
                "Next audit phases (after structural DWT + OpenJPEG baseline)" is dispositioned: promoted as lege-ecosystem.work.next-audit-phases-after-structural-dwt-openjpeg or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check pdf-renderer-follow-ups-outside-pure-codec-claim {
            statement """
                "PDF renderer follow-ups (outside pure codec)" is dispositioned: promoted as lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check phase-3-fused-packed-finalization-done-2026-08-claim {
            statement """
                "Phase 3 — Fused packed finalization — DONE (2026-08-05)" is dispositioned: promoted as lege-ecosystem.work.phase-3-fused-packed-finalization-done-2026-08 or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check phase-4-tier-1-tier-2-refinement-claim {
            statement """
                "Phase 4 — Tier-1 / Tier-2 refinement" is dispositioned: promoted as lege-ecosystem.work.phase-4-tier-1-tier-2-refinement or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check phase-5-true-windowed-line-based-roi-claim {
            statement """
                "Phase 5 — True windowed / line-based ROI" is dispositioned: promoted as lege-ecosystem.work.phase-5-true-windowed-line-based-roi or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check phase-6-isa-pgo-optional-gpu-claim {
            statement """
                "Phase 6 — ISA / PGO / optional GPU" is dispositioned: promoted as lege-ecosystem.work.phase-6-isa-pgo-optional-gpu or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
    }
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md"
    }
}

record lege-ecosystem.decode-optimization-handoff/1 : work {
    title "Import lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
    state completed
    intent """
        Disposition every durable claim of lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md (docs/12-migration.md).
        """
    acceptance {
        check degenerate-1-sample-dwt-split-8-files-resolved-claim {
            statement """
                "2. Degenerate 1-sample DWT split — ~8 files  [RESOLVED 2026-07-22 — root cause was a K/INV_K scaling on a one-row resolution in the *vertical* pass; see the update near the top. The "relaxing the bound drifts" note below is accurate but the cause is now fixed.]" is dispositioned: promoted as lege-ecosystem.requirement.degenerate-1-sample-dwt-split-8-files-resolved or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check end-to-end-oracle-whole-page-vs-pdfium-claim {
            statement """
                "End-to-end oracle (whole page vs PDFium)" is dispositioned: promoted as lege-ecosystem.work.end-to-end-oracle-whole-page-vs-pdfium or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check handoff-remaining-jp2lam-jpeg-2000-decode-gaps-claim {
            statement """
                "Handoff: remaining `jp2lam` JPEG 2000 decode gaps" is dispositioned: promoted as lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check implementation-update-claim {
            statement """
                "2026-07-19 implementation update" is dispositioned: promoted as lege-ecosystem.policy.implementation-update or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check later-update-section-2-resolved-and-sub-8-bit-claim {
            statement """
                "2026-07-22 (later) update — Section 2 RESOLVED, and sub-8-bit precision added" is dispositioned: promoted as lege-ecosystem.policy.later-update-section-2-resolved-and-sub-8-bit or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check orientation-environment-claim {
            statement """
                "0. Orientation & environment" is dispositioned: promoted as lege-ecosystem.requirement.orientation-environment or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check post-precinct-census-2026-07-19-from-pdf-claim {
            statement """
                "7. Post-precinct census (2026-07-19, from pdf-renderer) — new residuals" is dispositioned: promoted as lege-ecosystem.work.post-precinct-census-2026-07-19-from-pdf or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check precincts-sop-eph-10-files-largest-remaining-claim {
            statement """
                "1. Precincts / SOP / EPH — ~10 files (LARGEST remaining class)" is dispositioned: promoted as lege-ecosystem.requirement.precincts-sop-eph-10-files-largest-remaining or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check quick-reference-claim {
            statement """
                "6. Quick reference" is dispositioned: promoted as lege-ecosystem.work.quick-reference or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check run-the-census-2nd-arg-max-files-omit-for-all-claim {
            statement """
                "2. Run the census (2nd arg = max files; omit for all 424). ~250s per ~34 files." is dispositioned: promoted as lege-ecosystem.work.run-the-census-2nd-arg-max-files-omit-for-all or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check the-census-extraction-tool-your-main-instrument-claim {
            statement """
                "The census / extraction tool (your main instrument)" is dispositioned: promoted as lege-ecosystem.policy.the-census-extraction-tool-your-main-instrument or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check tpsot-tnsot-non-conformant-tile-parts-2-files-claim {
            statement """
                "4. `TPsot==TNsot` non-conformant tile-parts — ~2 files  [RESOLVED 2026-07-22 — the assembly-bug diagnosis in this section is WRONG; real cause was a missing `inalign` byte-realignment. See the 2026-07-22 update near the top.]" is dispositioned: promoted as lege-ecosystem.policy.tpsot-tnsot-non-conformant-tile-parts-2-files or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check two-traps-do-not-fix-these-by-loosening-a-check-claim {
            statement """
                "5. Two traps — do NOT "fix" these by loosening a check" is dispositioned: promoted as lege-ecosystem.work.two-traps-do-not-fix-these-by-loosening-a-check or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check update-sections-3-and-4-resolved-the-4-claim {
            statement """
                "2026-07-22 update — Sections 3 and 4 RESOLVED (the §4 diagnosis below was WRONG)" is dispositioned: promoted as lege-ecosystem.requirement.update-sections-3-and-4-resolved-the-4 or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check what-was-already-fixed-do-not-redo-claim {
            statement """
                "What was already fixed (do not redo)" is dispositioned: promoted as lege-ecosystem.work.what-was-already-fixed-do-not-redo or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
        check zero-length-code-block-contribution-truncated-claim {
            statement """
                "3. Zero-length code-block contribution (truncated streams) — ~6 files" is dispositioned: promoted as lege-ecosystem.work.zero-length-code-block-contribution-truncated or declined with evidence
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.decode-fix-plan-import-dispositioned-20260908/1
            ]
        }
    }
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
    }
}

record lege-ecosystem.encode-quality.improve-photographic-rate-control/1 : work {
    title "Rework jp2lam lossy rate control for perceptual, resolution-stable photographic quality"
    state proposed
    intent """
        Raise jp2lam lossy photographic quality-per-byte toward JPEG XL/BPG and make a fixed quality hold constant PERCEPTUAL quality across resolutions. The local allocator work is the existing perceptually weighted PCRD (size-matched Annex-J curves). The remaining encode-time quality objective is a SSIMULACRA2 score floor, not a butteraugli-anchored quality mapping: see lege-ecosystem.work.jp2lam-ssim-controller and lege-ecosystem.decision.jp2lam-ssim-canonical-metric. Grade screens with the pinned in-tree metric; keep butteraugli as a reported secondary column only.
        """
    note """
        Partial implementation landed: native lossy allocator size-matches perceptually weighted Annex-J curves to the established MSE byte target, maps luma masking through tile/subband origins, and uses smooth strength plus actual-bpp gates. Qualified same-size improvements at q25/q50/q75; q90 remains deliberately weak and mixed; q98 stays on the byte-identical baseline. The global quality controller is now tracked as lege-ecosystem.work.jp2lam-ssim-controller (shared JPXL SSIMULACRA2 metric; two-dimensional quant+PCRD search). Butteraugli is measurement-only.
        """
    supported_by [
        @lege-ecosystem.encode-quality.evidence.perceptual-pcrd-curve-2026-08-13/2,
        @lege-ecosystem.encode-quality.evidence.perceptual-pcrd-validation-2026-08-13/1
    ]
}

record lege-ecosystem.lege-process.about-gpu-init-2026-08-09/1 : work {
    title "Remove About email row and reduce Lege GPU initialization overhead"
    state superseded
    scope [
        path "lege-gpu/src/vision/runtime/device.rs",
        path "lege-process/GUI/Freya/src/**",
        path "lege-process/language_service/**/gui_text.json"
    ]
    intent "Remove the email line from the Freya About popup without residual layout space and evaluate GPU startup for measurable redundant initialization work, preserving adapter fallback and eager readiness semantics."
    note "GPU changes must retain hardware-first adapter policy, real fallback behavior, and fully initialized sessions; do not disguise cost through delayed initialization or reduced capability."
    acceptance {
        check about-email-removed {
            statement """
                The About popup contains no email row or placeholder spacing, and localized GUI text still deserializes.
                """
            method command
        }
        check focused-tests-pass {
            statement """
                Focused Freya, lege-gpu, and affected workspace checks pass.
                """
            method command
        }
        check gpu-initialization-evaluated {
            statement """
                GPU initialization is profiled and any retained optimization removes demonstrably redundant work without changing adapter fallback or eager session readiness.
                """
            method observation
        }
    }
}

record lege-ecosystem.lege-process.about-gpu-init-2026-08-09/2 : work {
    title "Remove About email row and reduce Lege GPU initialization overhead"
    state superseded
    scope [
        path "lege-gpu/src/vision/api.rs",
        path "lege-gpu/src/vision/runtime/compiled.rs",
        path "lege-gpu/src/vision/runtime/device.rs",
        path "lege-process/GUI/Freya/src/**",
        path "lege-process/GUI/musicsheet/src/**",
        path "lege-process/language_service/**/gui_text.json"
    ]
    intent "Remove the email line from both shipped Lege About popups without residual layout space and evaluate GPU startup for measurable redundant initialization work, preserving adapter fallback and eager readiness semantics."
    note "GPU changes must retain hardware-first adapter policy, real fallback behavior, and fully initialized sessions; do not disguise cost through delayed initialization or reduced capability."
    acceptance {
        check about-email-removed {
            statement """
                The About popup contains no email row or placeholder spacing, and localized GUI text still deserializes.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.lege-process-about-email-removed-2026-08-09/1
            ]
        }
        check focused-tests-pass {
            statement """
                Focused Freya, Sheet Music GUI, lege-gpu, and affected workspace checks pass.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.lege-process-about-gpu-focused-checks-2026-08-09/1
            ]
        }
        check gpu-initialization-evaluated {
            statement """
                GPU initialization is profiled and any retained optimization removes demonstrably redundant work without changing adapter fallback or eager session readiness.
                """
            method observation
            verified_by [ @lege-ecosystem.evidence.lege-gpu-layout-init-overlap-2026-08-09/1 ]
        }
    }
    supersedes [ @lege-ecosystem.lege-process.about-gpu-init-2026-08-09/1 ]
}

record lege-ecosystem.lege-process.about-gpu-init-2026-08-09/3 : work {
    title "Remove About email row and reduce Lege GPU initialization overhead"
    state superseded
    scope [
        path "lege-gpu/src/vision/api.rs",
        path "lege-gpu/src/vision/runtime/compiled.rs",
        path "lege-gpu/src/vision/runtime/device.rs",
        path "lege-process/GUI/Freya/src/**",
        path "lege-process/GUI/musicsheet/src/**",
        path "lege-process/language_service/**/gui_text.json"
    ]
    intent "Remove the email line from both shipped Lege About popups without residual layout space and evaluate GPU startup for measurable redundant initialization work, preserving adapter fallback and eager readiness semantics."
    note "GPU changes must retain hardware-first adapter policy, real fallback behavior, and fully initialized sessions; do not disguise cost through delayed initialization or reduced capability."
    acceptance {
        check about-email-removed {
            statement """
                The About popup contains no email row or placeholder spacing, and localized GUI text still deserializes.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.lege-process-about-email-removed-committed-2026-08-09/1
            ]
        }
        check focused-tests-pass {
            statement """
                Focused Freya, Sheet Music GUI, lege-gpu, and affected workspace checks pass.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.lege-process-about-gpu-checks-committed-2026-08-09/1
            ]
        }
        check gpu-initialization-evaluated {
            statement """
                GPU initialization is profiled and any retained optimization removes demonstrably redundant work without changing adapter fallback or eager session readiness.
                """
            method observation
            verified_by [
                @lege-ecosystem.evidence.lege-gpu-layout-init-overlap-committed-2026-08-09/1
            ]
        }
    }
    supersedes [ @lege-ecosystem.lege-process.about-gpu-init-2026-08-09/2 ]
}

record lege-ecosystem.lege-process.about-gpu-init-2026-08-09/4 : work {
    title "Remove About email row and reduce Lege GPU initialization overhead"
    state completed
    scope [
        path "lege-gpu/src/vision/api.rs",
        path "lege-gpu/src/vision/runtime/compiled.rs",
        path "lege-gpu/src/vision/runtime/device.rs",
        path "lege-process/GUI/Freya/src/**",
        path "lege-process/GUI/musicsheet/src/**",
        path "lege-process/language_service/**/gui_text.json"
    ]
    intent "Remove the email line from both shipped Lege About popups without residual layout space and evaluate GPU startup for measurable redundant initialization work, preserving adapter fallback and eager readiness semantics."
    note "GPU changes must retain hardware-first adapter policy, real fallback behavior, and fully initialized sessions; do not disguise cost through delayed initialization or reduced capability."
    acceptance {
        check about-email-removed {
            statement """
                The About popup contains no email row or placeholder spacing, and localized GUI text still deserializes.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.lege-process-about-email-removed-committed-2026-08-09/2
            ]
        }
        check focused-tests-pass {
            statement """
                Focused Freya, Sheet Music GUI, lege-gpu, and affected workspace checks pass.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.lege-process-about-gpu-checks-committed-2026-08-09/2
            ]
        }
        check gpu-initialization-evaluated {
            statement """
                GPU initialization is profiled and any retained optimization removes demonstrably redundant work without changing adapter fallback or eager session readiness.
                """
            method observation
            verified_by [
                @lege-ecosystem.evidence.lege-gpu-layout-init-overlap-committed-2026-08-09/3
            ]
        }
    }
    supersedes [ @lege-ecosystem.lege-process.about-gpu-init-2026-08-09/3 ]
}

record lege-ecosystem.work.about-image-pdf-agent-tool/1 : work {
    title "Fix embedded About access and Structures image detection; plan native PDF agent tool"
    state completed
    scope [
        path "lege-pdf/render/docs/agent-pdf-tool-plan.md",
        path "lege-process/**",
        path "lege-process/GUI/Freya/src/backend.rs"
    ]
    intent """
        Make compile-time About documents readable by sandboxed browsers, diagnose and correct reproducible image-region loss on Structures.pdf pages 190–210, and document a grounded native Lege PDF agent-tool path.
        """
    acceptance {
        check about-readable {
            statement """
                Embedded About documents are delivered without browser access to hidden cache files and the Freya tests pass.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.about-loopback-freya-tests/1 ]
        }
        check agent-tool-plan {
            statement """
                A Markdown plan describes benefits, limits, protocol, rollout, and validation for a native Lege PDF agent tool.
                """
            method manual
            verified_by [ @lege-ecosystem.evidence.native-pdf-agent-tool-plan/1 ]
        }
        check structures-diagnosed {
            statement """
                Structures.pdf pages 190–210 are traced through detection/postprocessing and any reproducible Lege filtering defect is narrowly fixed and tested.
                """
            method observation
            verified_by [ @lege-ecosystem.evidence.structures-image-overlay-range/1 ]
        }
    }
}

record lege-ecosystem.work.add-jp2decodestats/1 : work {
    title "3. Add `Jp2DecodeStats`"
    state abandoned
    intent """
        Do not start by optimizing `MqDecoder` merely because entropy decoding is normally expensive. The current profile identifies irreversible reconstruction, but Tier-1, dequantization, DWT, finalization, and copying are still combined too broadly.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Do not start by optimizing `MqDecoder` merely because entropy decoding is normally expensive. The current profile identifies irreversible reconstruction, but Tier-1, dequantization, DWT, finalization, and copying are still combined too broadly.
            """
    }
}

record lege-ecosystem.work.add-region-of-interest-decode-only-after/1 : work {
    title "32. Add region-of-interest decode only after resolution reduction"
    state abandoned
    intent """
        A PDF image is often clipped or only partly visible. JPEG 2000 precincts and code-blocks make partial spatial decode possible, but inverse DWT requires support samples beyond the exact output rectangle.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            A PDF image is often clipped or only partly visible. JPEG 2000 precincts and code-blocks make partial spatial decode possible, but inverse DWT requires support samples beyond the exact output rectangle.
            """
    }
}

record lege-ecosystem.work.approach-a-strided-vertical-kernels/1 : work {
    title "Approach A — Strided vertical kernels"
    state abandoned
    intent """
        Process several adjacent columns together so each cache line supplies useful samples for multiple independent columns.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Process several adjacent columns together so each cache line supplies useful samples for multiple independent columns.
            """
    }
}

record lege-ecosystem.work.approach-b-tiled-transpose/1 : work {
    title "Approach B — Tiled transpose"
    state abandoned
    intent """
        For each working-resolution rectangle:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            For each working-resolution rectangle:
            """
    }
}

record lege-ecosystem.work.approach-c-column-gather-into-reusable-lines/1 : work {
    title "Approach C — Column gather into reusable lines"
    state abandoned
    intent """
        Copy one or several columns into aligned line buffers, transform contiguously, then scatter them back.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Copy one or several columns into aligned line buffers, transform contiguously, then scatter them back.
            """
    }
}

record lege-ecosystem.work.auto-toc-all-outputs/1 : work {
    title "Complete automatic TOC and conservative document metadata"
    state completed
    scope [
        path "lege-codecs/djvulibrust/src/**",
        path "lege-pdf/read/src/**",
        path "lege-pdf/write/src/**",
        path "lege-process/core/djvu.rs",
        path "lege-process/core/toc.rs",
        path "lege-process/pipeline/**"
    ]
    intent """
        Complete the canonical automatic TOC plan across direct and reflow PDF, DjVu, and EPUB outputs; keep source outlines authoritative and add conservative source-preserving PDF title/author metadata inference.
        """
    acceptance {
        check djvu-tests {
            statement """
                DjVu manifest and NAVM tests pass.
                """
            method command
            command "cargo test --manifest-path lege-codecs/djvulibrust/Cargo.toml"
            verified_by [ @lege-ecosystem.evidence.auto-toc-djvu-tests/1 ]
        }
        check lege-tests {
            statement """
                Lege TOC, metadata, pipeline, and EPUB unit tests pass.
                """
            method command
            command "cargo test -p lege --lib"
            verified_by [ @lege-ecosystem.evidence.auto-toc-lege-lib-tests/1 ]
        }
        check pdf-tests {
            statement """
                PDF read/write outline and metadata tests pass.
                """
            method command
            command "cargo test -p lege-pdf-read -p lege-pdf-write"
            verified_by [ @lege-ecosystem.evidence.auto-toc-pdf-read-write-tests/1 ]
        }
        check workspace-check {
            statement """
                The workspace compiles.
                """
            method command
            command "cargo check --workspace"
            verified_by [ @lege-ecosystem.evidence.auto-toc-workspace-check/1 ]
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    source {
        kind internal
        role origin
        path "to-do-plans/auto-toc-plan.md"
        use """
            Canonical design and phased implementation requirements for source-outline preservation, synthesis, DjVu NAVM, and EPUB unification.
            """
    }
}

record lege-ecosystem.work.auto-width-about-dialog/1 : work {
    title "Auto-size the compact About dialog"
    state completed
    scope [ path "lege-process/GUI/Freya/src/app.rs" ]
    intent """
        Allow the normal About dialog to shrink to its version header and compact action row while keeping the theme chooser wide enough for its controls.
        """
    acceptance {
        check about-auto-width {
            statement """
                The normal About content uses automatic width, the chooser retains its fixed width, and focused Freya tests pass.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.about-auto-width-freya-test/1 ]
        }
    }
}

record lege-ecosystem.work.component-level-parallelism/1 : work {
    title "26. Component-level parallelism"
    state abandoned
    intent """
        Before inverse MCT, grayscale/RGB/CMYK component reconstruction is independent.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Before inverse MCT, grayscale/RGB/CMYK component reconstruction is independent.
            """
    }
}

record lege-ecosystem.work.core-recommendation/1 : work {
    title "39. Core recommendation"
    state abandoned
    intent """
        The optimization should not begin as “add SIMD to the decoder.”
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The optimization should not begin as “add SIMD to the decoder.”
            """
    }
}

record lege-ecosystem.work.current-decoder-pipeline/1 : work {
    title "2. Current decoder pipeline"
    state abandoned
    intent """
        The current top-level path is:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The current top-level path is:
            """
    }
}

record lege-ecosystem.work.do-not-start-with/1 : work {
    title "Do not start with"
    state abandoned
    intent """
        - MQ symbol micro-opts
        - Nested Rayon inside page-parallel renders without a global permit
        - ROI as “decode full, crop”
        - Changing 9/7 rounding for speed without OpenJPEG differential tests
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md"
        excerpt """
            - MQ symbol micro-opts
            - Nested Rayon inside page-parallel renders without a global permit
            - ROI as “decode full, crop”
            - Changing 9/7 rounding for speed without OpenJPEG differential tests
            """
    }
}

record lege-ecosystem.work.document-ocr-cli-native-text-backend-init/1 : work {
    title "Decide whether a native-text-only OCR job may run without an available OCR backend"
    state proposed
    scope [
        path "lege-document-ocr/cli/src/main.rs",
        path "lege-document-ocr/crates/pipeline/src/lib.rs"
    ]
    intent """
        cargo test -p lege-document-ocr-cli fails on any non-Windows host, and it is the one unsatisfied
        acceptance check on the Business-grade PDF OCR CLI milestone.

        WHAT HAPPENS: tests::native_text_batch_exports_and_resumes_without_invoking_ocr builds a PDF with
        a real text layer and runs the batch with BackendArg::WinocrLegacy, deliberately choosing a
        backend that is unavailable so that reaching OCR at all would fail loudly. But
        DocumentProcessor::new initializes the backend eagerly
        (lege-document-ocr/crates/pipeline/src/lib.rs:129-133, initialize_backend at line 1018), so
        construction returns UnavailableBackend("winocr-legacy") before any page is classified and
        run_batch panics at lege-document-ocr/cli/src/main.rs:849. The test was added in 27900f0e, after
        b6c6ae48 made initialization eager, so it has never passed off Windows.

        WHY THIS IS NOT A ONE-LINE FIX, AND WHAT THE NEXT SESSION MUST DECIDE. There is a product
        question underneath the failing test: should a document whose pages all carry a native text layer
        require a working OCR backend at all? Three answers, and they are not equivalent:

        (a) Keep eager initialization and mark the test #[cfg(target_os = "windows")]. Smallest diff.
        Costs the assertion on Linux, where it becomes vacuous, and leaves the underlying behaviour
        (a text-only job refusing to start on a host with no OCR backend) in place.

        (b) Make backend initialization lazy, so the error surfaces only when a page actually needs OCR.
        This is what the test's name asserts and what the vestigial DocumentProcessor::backend() ->
        Result signature at lib.rs:180 was shaped for. The obstacle is that new() also uses the resolved
        backend to rewrite config.backend, config.tensorrt_paddle, the model identity and the selection
        warning; config.backend then feeds configuration_hash(), which is the resume/checkpoint identity.
        Deferring initialization means the recorded backend is the requested one rather than the resolved
        one, which silently changes when a resumed job is considered to have the same configuration. That
        seam has to be designed, not patched.

        (c) Keep eager initialization but let it degrade: construct successfully with no backend, record
        the reason, and fail only at the point of use. Same checkpoint-identity question as (b), plus
        selected_backend_name() and the Debug impl need a defined answer when there is no backend.

        Nothing here is urgent -- the shipped Windows path is unaffected -- but the milestone cannot be
        completed until it is settled, and the check is left deliberately unsatisfied rather than
        weakened.
        """
}

record lege-ecosystem.work.document-ocr-layout-detection/1 : work {
    title "Layout detection (PP-DocLayout-M) in the standalone document OCR tool"
    state completed
    intent """
        Integrate PP-DocLayout-M layout detection into the standalone lege-ocr document pipeline (embedded model, --layout-model override, --no-layout opt-out): drop page furniture (header/footer/number), map doc/paragraph titles to Title/Heading regions, emit placeholder text for content images, and keep the mapping open to every other layout class. Verify end to end on a real PDF from ~/Downloads.
        """
    acceptance {
        check layout-e2e {
            statement """
                Batch OCR on a ~/Downloads PDF produces text/markdown with page furniture removed, chapter titles as headings, and image placeholders
                """
            method command
            verified_by [ @lege-ecosystem.evidence.document-ocr-layout-e2e-2026-09-19/1 ]
        }
        check layout-unit-tests {
            statement """
                Pipeline, export, and docir unit tests pass, including layout mapping tests
                """
            method command
            verified_by [ @lege-ecosystem.evidence.document-ocr-layout-unit-tests/1 ]
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli ]
}

record lege-ecosystem.work.dwt-row-and-column-parallelism/1 : work {
    title "27. DWT row and column parallelism"
    state abandoned
    intent """
        Within one large component:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Within one large component:
            """
    }
}

record lege-ecosystem.work.eliminate-temporary-full-image-allocation-for/1 : work {
    title "9. Eliminate temporary full-image allocation for single-tile images"
    state abandoned
    intent """
        The current `decode_jp2()` allocates the final image before decoding and then creates a complete temporary tile image, which is copied into it.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The current `decode_jp2()` allocates the final image before decoding and then creates a complete temporary tile image, which is copied into it.
            """
    }
}

record lege-ecosystem.work.embed-freya-about-documents/1 : work {
    title "Embed Freya About documents and separate GUI build aliases"
    state completed
    scope [
        path ".cargo/config.toml",
        path "lege-misc/docs/documentation.html",
        path "lege-misc/docs/licenses.html",
        path "lege-process/GUI/Freya/src/app.rs",
        path "lege-process/GUI/Freya/src/backend.rs",
        path "lege-process/core/main.rs"
    ]
    intent """
        Make Freya About documentation and license actions independent of release-folder copies, keep one compile-time source of truth, and make cargo gui build Freya while cargo viewer-gui builds the native viewer.
        """
    note """
        The pre-change project, Linux staging, and MSIX documentation files were byte-identical and included Reflow. The project license page was newer than staging and became the canonical audited source.
        """
    claim build-aliases {
        text """
            cargo gui builds the Lege worker and integrated Freya GUI; cargo viewer-gui builds the separate native viewer.
            """
    }
    claim current-notices {
        text """
            The focused license notice names default PP-OCRv5/wgpu and current codecs while removing unused ort/lopdf and describing Tesseract as optional legacy functionality.
            """
    }
    claim embedded-about {
        text """
            Freya embeds documentation.html, licenses.html, and the full AGPL text at compile time, materializing only disposable cache files for the system browser.
            """
    }
    claim opener-errors {
        text """
            About buttons close only after a successful system opener result and otherwise show the actual error.
            """
    }
    acceptance {
        check document-audit {
            statement """
                Before editing, project/Linux/MSIX documentation copies had identical SHA-256 c78277275d80915433e541dc2e712d18281dd19c7a855b66e0145929d84c9e5a and included the Reflow section.
                """
            method observation
            verified_by [ @lege-ecosystem.evidence.release-document-copy-audit/1 ]
        }
        check embedded-output {
            statement """
                Built artifacts contain the canonical documents and the CLI prints the embedded updated notice.
                """
            method command
            command "strings target/debug/lege-gui | rg -m 2 'Margin Correction and Reflow|Lege License and Third-Party Notices'; target/debug/lege --licenses | rg 'PaddleOCR / PP-OCRv5'"
            verified_by [ @lege-ecosystem.evidence.embedded-about-built-output/1 ]
        }
        check freya-tests {
            statement """
                The complete Freya suite passes, including embedded-content and stale-cache refresh tests.
                """
            method command
            command "cargo test -p lege-gui-freya"
            verified_by [ @lege-ecosystem.evidence.freya-embedded-about-tests/1 ]
        }
        check gui-builds {
            statement """
                Both requested GUI build aliases complete successfully and select their intended applications.
                """
            method command
            command "cargo gui && cargo viewer-gui"
            verified_by [ @lege-ecosystem.evidence.freya-viewer-build-aliases/1 ]
        }
    }
}

record lege-ecosystem.work.end-to-end-oracle-whole-page-vs-pdfium/1 : work {
    title "End-to-end oracle (whole page vs PDFium)"
    state abandoned
    intent """
        `pdf-renderer/tools/pdfium-diff` (a separate crate, needs `libpdfium`;
        `D:\Rust-projects\pdfium-port-plan\pdfium.dll` is present):
        ```bash
        cd tools/pdfium-diff
        cargo run -- D:/Rust-projects/pdfium-port-plan/pdfium.dll 2.0 "<one-file.pdf>"
        # writes pdfium-diff-out/results.csv; summary line prints suspect/silent-blanks/degraded.
        ```
        Use this to confirm a decode fix actually clears the page against PDFium. **Do
        not run the full corpus sweep** (11k files, ~2 h) — the owner runs that; single
        files are seconds.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
        excerpt """
            `pdf-renderer/tools/pdfium-diff` (a separate crate, needs `libpdfium`;
            `D:\Rust-projects\pdfium-port-plan\pdfium.dll` is present):
            ```bash
            cd tools/pdfium-diff
            cargo run -- D:/Rust-projects/pdfium-port-plan/pdfium.dll 2.0 "<one-file.pdf>"
            # writes pdfium-diff-out/results.csv; summary line prints suspect/silent-blanks/degraded.
            ```
            Use this to confirm a decode fix actually clears the page against PDFium. **Do
            not run the full corpus sweep** (11k files, ~2 h) — the owner runs that; single
            files are seconds.
            """
    }
}

record lege-ecosystem.work.evidence-broker-ocr-provider/1 : work {
    title "Route Lege page OCR through the Evidence broker"
    state completed
    scope [ path "lege-document-ocr/**", path "lege-ocr/**" ]
    intent """
        Add a brokered TensorRT page-OCR provider that preserves Lege's PDF, scheduling, checkpoint, DocIR and exporter ownership while delegating live OCR inference to the versioned Evidence broker.
        """
    acceptance {
        check broker-provider {
            statement """
                The Lege CLI processes a PDF through the Evidence broker provider, records the pinned model revision in DocIR, and resumes without starting a second native TensorRT worker.
                """
            method command
            command "cargo test -p lege-ocr -p lege-document-ocr-cli"
            verified_by [ @lege-ecosystem.evidence.evidence-broker-ocr-provider-20260822/1 ]
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    author "GitHub Uploader"
}

record lege-ecosystem.work.fixed-point-9-7-is-a-later-experiment/1 : work {
    title "19. Fixed-point 9/7 is a later experiment"
    state abandoned
    intent """
        A fixed-point inverse 9/7 implementation may improve speed and reproducibility, but it will probably change some output samples.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            A fixed-point inverse 9/7 implementation may improve speed and reproducibility, but it will probably change some output samples.
            """
    }
}

record lege-ecosystem.work.fold-automatic-toc-into-ocr/1 : work {
    title "Fold automatic table of contents into the GUI OCR feature"
    state superseded
    scope [
        path "lege-process/GUI/Freya/src/**",
        path "lege-process/language_service/*/gui_text.json"
    ]
    intent """
        Make the Freya OCR control own search-overlay, synthesized-TOC, and metadata generation without a separate automatic-TOC GUI setting.
        """
    note """
        The standalone core CLI --auto-toc switch remains available; this change concerns the Freya product model.
        """
    claim both-modes {
        text """
            Both Fast and Thorough OCR modes request automatic TOC generation, while disabled OCR does not.
            """
    }
    claim single-control {
        text """
            Freya presents no separate automatic table-of-contents control or persisted GUI state.
            """
    }
    claim tooltip {
        text """
            The English OCR tooltip states that OCR generates the search overlay, TOC, and metadata.
            """
    }
    acceptance {
        check gui-behavior {
            statement """
                The Freya unit suite passes, including OCR-to-TOC worker argument coverage for Fast, Thorough, and disabled OCR.
                """
            method command
            command "cargo test -p lege-gui-freya"
        }
        check locale-schema {
            statement """
                Bundled GUI locale JSON remains syntactically valid and matches the Rust schema.
                """
            method command
            command "python3 -m json.tool lege-process/language_service/en/gui_text.json >/dev/null && python3 -m json.tool lege-process/language_service/de/gui_text.json >/dev/null && cargo test -p lege-gui-freya gui_text::tests::bundled_locales_match_the_gui_schema"
        }
    }
    derived_from [ @lege-ecosystem.work.make-automatic-toc-explicit/1 ]
}

record lege-ecosystem.work.fold-automatic-toc-into-ocr/2 : work {
    title "Fold automatic table of contents into the GUI OCR feature"
    state completed
    scope [
        path "lege-process/GUI/Freya/src/**",
        path "lege-process/language_service/*/gui_text.json"
    ]
    intent """
        Make the Freya OCR control own search-overlay, synthesized-TOC, and metadata generation without a separate automatic-TOC GUI setting.
        """
    note """
        The standalone core CLI --auto-toc switch remains available; this change concerns the Freya product model.
        """
    claim both-modes {
        text """
            Both Fast and Thorough OCR modes request automatic TOC generation, while disabled OCR does not.
            """
    }
    claim single-control {
        text """
            Freya presents no separate automatic table-of-contents control or persisted GUI state.
            """
    }
    claim tooltip {
        text """
            The English OCR tooltip states that OCR generates the search overlay, TOC, and metadata.
            """
    }
    acceptance {
        check gui-behavior {
            statement """
                The Freya unit suite passes, including OCR-to-TOC worker argument coverage for Fast, Thorough, and disabled OCR.
                """
            method command
            command "cargo test -p lege-gui-freya"
            verified_by [ @lege-ecosystem.evidence.ocr-owned-toc-gui-suite/1 ]
        }
        check locale-schema {
            statement """
                Bundled GUI locale JSON remains syntactically valid and matches the Rust schema.
                """
            method command
            command "python3 -m json.tool lege-process/language_service/en/gui_text.json >/dev/null && python3 -m json.tool lege-process/language_service/de/gui_text.json >/dev/null && cargo test -p lege-gui-freya gui_text::tests::bundled_locales_match_the_gui_schema"
            verified_by [ @lege-ecosystem.evidence.ocr-owned-toc-locales/1 ]
        }
    }
    derived_from [ @lege-ecosystem.work.make-automatic-toc-explicit/1 ]
    supersedes [ @lege-ecosystem.work.make-automatic-toc-explicit/1 ]
}

record lege-ecosystem.work.full-resolution-correctness/1 : work {
    title "34. Full-resolution correctness"
    state abandoned
    intent """
        For the legacy `decode_jp2()` path require:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            For the legacy `decode_jp2()` path require:
            """
    }
}

record lege-ecosystem.work.full-resolution-path/1 : work {
    title "Full-resolution path"
    state abandoned
    intent """
        * JPX decode below 220 ms without internal parallelism.
        * JPX decode below 150–180 ms with a modest thread budget.
        * At least 40–50% reduction in decoder-owned peak intermediate memory.
        * Byte-identical legacy output.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            * JPX decode below 220 ms without internal parallelism.
            * JPX decode below 150–180 ms with a modest thread budget.
            * At least 40–50% reduction in decoder-owned peak intermediate memory.
            * Byte-identical legacy output.
            """
    }
}

record lege-ecosystem.work.fuse-operations-where-useful/1 : work {
    title "18. Fuse operations where useful"
    state abandoned
    intent """
        Potential fusions to benchmark:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Potential fusions to benchmark:
            """
    }
}

record lege-ecosystem.work.fuse-tier-1-output-and-dequantization/1 : work {
    title "11. Fuse Tier-1 output and dequantization"
    state abandoned
    intent """
        The current irreversible flow is:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The current irreversible flow is:
            """
    }
}

record lege-ecosystem.work.glyph-font-text-output/1 : work {
    title "Emit printed text as an embedded per-document glyph font (text_format glyphfont)"
    state proposed
    scope [
        path "lege-document-ocr/crates/export/src/lib.rs",
        path "lege-pdf/read/src/session.rs",
        path "lege-pdf/write/**",
        path "lege-process/GUI/Freya/**",
        path "lege-process/core/accumulator.rs",
        path "lege-process/core/clean_gray.rs",
        path "lege-process/core/main.rs",
        path "lege-process/core/pdf_artifact.rs",
        path "lege-process/core/truetype_writer.rs",
        path "lege-process/encoding/**",
        path "lege-process/language_service/**",
        path "lege-process/pipeline/config.rs",
        path "lege-process/pipeline/helper_functions.rs",
        path "lege-process/pipeline/pdf_tokio_pipeline.rs"
    ]
    intent """
        Replace the JBIG2/CCITT raster text layer with a PDF text object drawn from one document-wide TrueType font whose glyphs are clustered connected components of the binarized pages. Staged: (1) pixel-exact staircase outlines, first-seen prototypes, incremental document-wide dictionary, writer emits the font at finalize under a reserved id; (2) font-oriented clustering: majority-vote prototypes, edge-tolerant structural matching, alias merging into TrueType composites, page-median descent and advance; (3) quadratic Bezier outlines fitted to the prototypes with a raster-back check and staircase fallback; (4) ToUnicode by aligning glyph placements to OCR words, replacing the invisible OCR layer on glyph pages; (5) text orientation and skew straightening from the components themselves, and deduplication under the printing-press prior (a book has one piece of type per letter, so like shapes inked differently are one glyph); (6) text at any angle, the same orientation measurement turning pages upright for OCR in every text format, and a matching path fast enough not to matter (no rayon; word-parallel kernels); (7) the rest of the page's cost at the raised render height: the renderer's fast paths for gray scans, the preserved mask's coverage plane, and the background encoded at the height the caller asked for.
        """
    note """
        Pass 9 (2026-09-03) answered the two failures the user hit on the day truetyping became the default. Wrong letters: the gates could forgive a whole stroke, because a hairline is invisible to a measure of 2x2 blocks and a crossbar four pixels long has both ends touching the bowl. Counters are now identity (no fold across a different number of them, where the strokes are thick enough for the count to mean anything) and the strict gate gained the tolerant gate's far-pixel test. T.88's body text is now correct; what remains is a few substitutions in small bold type. The 836-page failure was the glyph id space, so the session keeps a dictionary per font bank and a page draws from one font; the same book also revealed that rendering an already-binarized source above its own resolution invents edges, so truetyping now traces such a book at the resolution it has. The book that failed after 7 minutes now finishes in 3 with two fonts. The writer actor also records what it died of, since the sender used to report only that the channel had closed. Costs: dictionaries grow 3-10%, Gobineau word recall 0.845 to 0.829-0.833, which is inside its run-to-run band.
        """
    claim the-user-found-it-on-two-books {
        text """
            The default's first day in a user's hands found both failures at once: the ITU T.88 specification came back with whole letters replaced in body text, and an 836-page book died after seven minutes on the 16-bit glyph id space while taking twenty minutes to get halfway.
            """
    }
    claim upscaling-a-binarized-scan-is-not-free {
        text """
            Rendering a book that is already black and white above its own resolution is actively harmful: on a 1200 px source at 2400 px, 120 pages made 24 381 glyphs in 3 minutes; at 1400 px they made 9 363 in 18 seconds, and the text read better.
            """
    }
    depends_on [ @lege-ecosystem-perf.renderer.gray8-draws-take-the-rgb8-fast-paths ]
    implements [ @lege-ecosystem.decision.jbig2-is-the-default-pdf-text-format-again ]
}

record lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps/1 : work {
    title "Handoff: remaining `jp2lam` JPEG 2000 decode gaps"
    state proposed
    intent """
        **Audience:** an agent continuing the JPX decode work in `jp2lam`.
        **Context:** `jp2lam` is the JPEG 2000 codec behind the `pdf-renderer` port of
        PDFium. Its decoder feeds `/JPXDecode` images. A differential sweep of
        `pdf-renderer` against PDFium found ~610 corpus pages rendering **blank**
        because their JPX image failed to decode and the error was swallowed. That has
        been root-caused and mostly fixed; this document hands off the residual tail.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
        excerpt """
            **Audience:** an agent continuing the JPX decode work in `jp2lam`.
            **Context:** `jp2lam` is the JPEG 2000 codec behind the `pdf-renderer` port of
            PDFium. Its decoder feeds `/JPXDecode` images. A differential sweep of
            `pdf-renderer` against PDFium found ~610 corpus pages rendering **blank**
            because their JPX image failed to decode and the error was swallowed. That has
            been root-caused and mostly fixed; this document hands off the residual tail.
            """
    }
}

record lege-ecosystem.work.horizontal-lifting-kernels/1 : work {
    title "15. Horizontal lifting kernels"
    state abandoned
    intent """
        Horizontal rows are naturally contiguous. Implement specialized kernels for:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Horizontal rows are naturally contiguous. Implement specialized kernels for:
            """
    }
}

record lege-ecosystem.work.initial-target-results/1 : work {
    title "37. Initial target results"
    state abandoned
    intent """
        Use the current measured values as the first scoreboard:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Use the current measured values as the first scoreboard:
            """
    }
}

record lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan/1 : work {
    title "jp2lam decoder optimisation: the remaining tail"
    state proposed
    scope [
        path "lege-codecs/jp2lam/src/**",
        path "lege-pdf/render/crates/pdf-image/src/jpx.rs"
    ]
    intent """
        The single live record for the jp2lam JPEG 2000 decoder optimisation effort. It
        replaces the ~125 work records the 2026-08 bulk import created from the five
        planning documents in lege-codecs/jp2lam/decode-fix-plan/; those were abandoned on
        2026-09-08 as mis-imported markdown headings. The documents themselves stay on disk
        and remain the detailed authority.

        Status re-verified against lege-codecs/jp2lam/src on 2026-09-08 (repo HEAD 823d46dd).

        LANDED, confirmed in current source:
        - Jp2DecodeStats stage attribution (src/decode/stats.rs).
        - Renderer-oriented decode API: DecodeRequest / DecodeResolution / DecodeOutputFormat /
          Jp2Decoder, plus decode_into() writing straight into caller memory (src/decode/mod.rs).
        - Reduced-resolution decode, including multi-tile (select_reduce_levels,
          reduced_axis_bounds in src/decode/mod.rs).
        - Tier-1-fused dequantization with no separate full coefficient plane
          (src/decode/reconstruct.rs, src/decode/t1.rs).
        - Fused packed output with byte-identity tests, and a direct sYCC 4:2:0 kernel
          (reconstruct_fused_packed_into in src/decode/reconstruct.rs).
        - Banded vertical 9/7 reconstruction at VERTICAL_BAND_COLS = 128 and coarse-chunk
          horizontal scratch (src/dwt/irrev97.rs).
        - Lattice-aligned nonzero tile-origin routing into the optimized inverse DWT
          (lattice_aligned_origin in src/decode/reconstruct.rs).
        - Weighted largest-processing-time Tier-1 scheduling over a shared work cursor with
          one persistent Tier1Scratch per pool worker (decode_codeblocks_parallel in
          src/decode/t1.rs), bit-exact against the serial path.
        - Tier-2 allocation hygiene: range-based precinct block membership, streaming
          PacketStats instead of retained packet records, try_reserve from header-derived
          counts (src/decode/t2.rs).
        - Renderer integration: thread-local persistent Jp2Decoder, DecodeConcurrency::Budgeted,
          DecodeResolution::AtLeast from the device footprint
          (lege-pdf/render/crates/pdf-image/src/jpx.rs).
        - All four correctness classes of HANDOFF-remaining-jpx-decode.md (precincts/SOP/EPH,
          degenerate 1-sample DWT, zero-length contribution for the named repros, TPsot==TNsot
          tail) were resolved before the 2026-08-05 audit was written.

        NEGATIVE RESULT, do not re-land as-is: the wide f32x8 fused ICT/clamp/interleave SIMD
        experiment (commit c2c8dbf1) was performance-neutral and byte-identical because fat-LTO
        LLVM already vectorizes the scalar loop; the stage is memory-bandwidth-bound.

        GENUINELY OPEN, in the order the plan itself recommends:
        1. True windowed / line-based ROI decode (plan Milestone 8, audit section 10,
           NEXT-audit-phases Phase 5). Today ROI runs the full inverse DWT per tile and clips
           while packing; see the comment at src/decode/mod.rs around line 421. The interim
           region-sized destination allocation landed, the windowed subband IDWT with halo did
           not.
        2. Tier-2 cleanups: the merged-block HashMap<(usize,usize), MergedBlock> at
           src/decode/t2.rs:268 is still a HashMap rather than a dense index, and
           build_packet_positions still eagerly sorts packet positions instead of iterating a
           progression order.
        3. Tier-1 style specialization, compact FlagGrid words and stripe-major coefficient
           layout (plan sections 20-24). Only the stripe_is_clean fast path exists
           (src/tier1/flags.rs).
        4. Architecture-specific SIMD and PGO (plan Milestone 5 remainder,
           NEXT-audit-phases Phase 6). src/simd/x86/mod.rs still carries an empty AVX2 shell:
           "No AVX2 overrides are installed." No PGO build configuration and no GPU backend.
        5. Small renderer follow-ups: Rgbx8/Bgra8 exist in the codec but jpx.rs maps only
           Gray8/GrayA8/Rgb8/Rgba8/Cmyk8, so a 4-byte-pixel GPU compositing path still swizzles.
        6. sYCC 4:2:2 and 4:4:4 direct kernels (only 4:2:0 landed).
        7. Tile-part QCC component quantization override is still rejected outright
           (MARKER_QCC in src/j2k/decode_markers.rs), one of the post-precinct census residuals.

        WHAT THE NEXT SESSION MUST DECIDE FIRST: item 1 and item 4 are the two the plan itself
        called the hardest and deferred, and both are profiling-gated. The detailed
        DWT/Tier-1/final-pack flamegraph predates the 128-column vertical band and the
        direct-output change, so refresh stage attribution with the current decode_benchmark
        before picking any hot-loop target. Items 2, 3, 5, 6 and 7 are independently landable
        without that.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
    }
}

record lege-ecosystem.work.jp2lam-ssim-controller/1 : work {
    title "jp2lam SSIMULACRA2 score-floor quality controller"
    state proposed
    intent """
        Give jp2lam a SSIMULACRA2 score-floor quality controller: the smallest exact stream, within a bounded probe budget, whose reconstructed pixels meet a requested minimum score. Share JPXL's pinned production metric (`ssimulacra2-jpxl-1` in jpxl-perceptual) rather than a second implementation. Port JPXL controller mathematics (log-loss crossing, geometric bracket, work budgets, refuse-under-target) into a JPEG 2000-native two-dimensional search: outer global quantizer scale, inner PCRD body-bytes truncation over stored Tier-1 passes. Keep existing perceptual PCRD as the local allocator. Do not port the JPXL predictor, quantizer ladder, or structure-reuse rules. Do not change RateControl::Quality(u8) semantics until a later mapping session. v1 is Sessions 1-5 of the 2026-09-04 implementation plan (shared metric, Perceptual API, slow evaluator, PCRD-only proof, outer QuantRung).
        """
    note """
        Session 1 (2026-09-04) landed: jpxl-perceptual default path no longer requires jpxl-core (AVX2 dispatch uses a local cpu module; jpxl-core is evaluator-only). jp2lam path-deps jpxl-perceptual with default-features=false, re-exports METRIC_VERSION, and has tests/metric_identity.rs. MSRV stays 1.95. Next: Session 2 RateControl::Perceptual types, no search.
        """
}

record lege-ecosystem.work.layout-vertical-bands-ocr-horizontal/1 : work {
    title "Correct DocLayout underdetection with vertical bands"
    state proposed
    intent """
        After DocLayout NMS, keep the model's vertical text bands, expand those boxes horizontally from OCR-det line boxes plus ink, insert synthetic text boxes for uncovered ink/OCR gaps, grow image boxes to nearby illustration pixels, and optionally re-detect on an inverted raster when a large uncovered high-variance region looks like a missed plate. Apply the same correction in `--layout-visualize`.
        """
    note """
        Implemented in lege-process/core/layout_correct.rs and hooked from LayoutEngine::detect_single_blocking. LEGE_LAYOUT_OCR_DET=0 skips the extra DBNet pass; LEGE_LAYOUT_IMAGE_REDETECT=0/1 forces the invert pass off/on.
        """
}

record lege-ecosystem.work.level-local-operation/1 : work {
    title "17. Level-local operation"
    state abandoned
    intent """
        Do not run kernels over unused full-image regions.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Do not run kernels over unused full-image regions.
            """
    }
}

record lege-ecosystem.work.line-art-variance-postprocess/1 : work {
    title "Skip line-art image detections via bpg-rs variance classifier"
    state proposed
    intent """
        After DocLayout image-class detections, classify each box with the bpg-rs 32x32 variance / Sobel / orientation preanalysis. Skip line-art boxes as raster overlays so they are binarized with the page or carried by the MRC JBIG2 mask; keep photographs, maps, and engravings as overlays. Do not revive the luma-histogram bimodality test removed in 2a86e46.
        """
    note """
        Classifier lives in lege-process/core/content_class.rs (thresholds copied from bpg-rs still265 preanalysis). PDF and DJVU pipelines call should_keep_image_overlay before premask. layout_correct also demotes line-art image boxes to text.
        """
    author "GitHub Uploader"
}

record lege-ecosystem.work.linux-gui-backend-fallback/1 : work {
    title "Make Linux GUI event-loop startup fall back from unavailable Wayland to X11"
    state completed
    scope [ path "freya-main/crates/freya-winit/**", path "lege-process/GUI/Freya/**" ]
    intent """
        Make Linux GUI startup select an actually usable winit backend and recover from an unavailable Wayland compositor by trying X11 when available, while preserving explicit backend choices.
        """
    claim intent {
        text """
            Linux GUI startup must choose an actually usable winit backend and recover from an unavailable Wayland compositor by trying X11 when available, while preserving explicit backend choices.
            """
    }
    acceptance {
        check backend-policy {
            statement """
                Unit tests cover automatic Wayland preference, stale-Wayland-to-X11 fallback, single advertised backend selection, and the no-backend error.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.linux-gui-backend-policy-tests/1 ]
        }
        check compile {
            statement """
                The affected Freya winit crate and the Lege Freya GUI compile with the Linux backend-selection implementation.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.linux-gui-freya-build/1 ]
        }
        check startup-diagnostic {
            statement """
                With a stale WAYLAND_DISPLAY and a live DISPLAY the GUI enters its event loop through X11, while exhausting both backends reports a specific diagnostic instead of the opaque winit expect.
                """
            method observation
            verified_by [ @lege-ecosystem.evidence.linux-gui-stale-wayland-fallback/1 ]
        }
    }
}

record lege-ecosystem.work.make-automatic-toc-explicit/1 : work {
    title "Make synthesized table of contents an explicit option"
    state superseded
    scope [
        path "lege-process/GUI/Freya/src/**",
        path "lege-process/core/main.rs",
        path "lege-process/core/processing_log.rs",
        path "lege-process/language_service/**",
        path "lege-process/lege-ipc/src/processing_log.rs",
        path "lege-process/pipeline/*_pipeline.rs",
        path "lege-process/pipeline/config.rs"
    ]
    intent """
        Gate synthesized PDF and DjVu outlines behind an explicit opt-in while preserving source outlines independently.
        """
    note """
        Deliver a GUI Automatic table of contents option and CLI --auto-toc flag with explicit GUI OCR/layout dependencies.
        """
    claim explicit-opt-in {
        text """
            Synthesized outlines are disabled by default and enabled only by the automatic TOC option.
            """
    }
    claim source-outline-independent {
        text """
            Source-provided outlines remain preserved regardless of the automatic TOC setting.
            """
    }
    claim visible-dependencies {
        text """
            The Freya GUI makes automatic TOC dependence on OCR and layout detection visible and keeps those settings coherent.
            """
    }
    acceptance {
        check core-gating {
            statement """
                Core TOC synthesis tests pass with capture sites gated by the opt-in configuration.
                """
            method command
            command "cargo test -p lege toc --lib"
            verified_by [ @lege-ecosystem.evidence.automatic-toc-core-gating/1 ]
        }
        check gui-dependencies {
            statement """
                GUI option, normalization, localization schema, and worker argument tests pass.
                """
            method command
            command "cargo test -p lege-gui-freya"
            verified_by [ @lege-ecosystem.evidence.automatic-toc-gui-dependencies/1 ]
        }
        check workspace-regression {
            statement """
                The workspace test suite completes without failures.
                """
            method command
            command "cargo test --workspace --no-fail-fast"
            verified_by [ @lege-ecosystem.evidence.automatic-toc-workspace-regression/1 ]
        }
    }
}

record lege-ecosystem.work.material-needed-by-the-implementing-agent/1 : work {
    title "38. Material needed by the implementing agent"
    state abandoned
    intent """
        The supplied files expose the decoder orchestration, Tier-1 loops, Tier-2 packet handling, and reconstruction pipeline. The exact low-level optimization work also requires:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The supplied files expose the decoder orchestration, Tier-1 loops, Tier-2 packet handling, and reconstruction pipeline. The exact low-level optimization work also requires:
            """
    }
}

record lege-ecosystem.work.milestone-1-decoder-attribution/1 : work {
    title "Milestone 1 — Decoder attribution"
    state abandoned
    intent """
        Implement:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Implement:
            """
    }
}

record lege-ecosystem.work.milestone-2-remove-obvious-duplication/1 : work {
    title "Milestone 2 — Remove obvious duplication"
    state abandoned
    intent """
        Implement:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Implement:
            """
    }
}

record lege-ecosystem.work.milestone-3-fused-tier-1-output/1 : work {
    title "Milestone 3 — Fused Tier-1 output"
    state abandoned
    intent """
        Implement:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Implement:
            """
    }
}

record lege-ecosystem.work.milestone-4-reduced-resolution-decode/1 : work {
    title "Milestone 4 — Reduced-resolution decode"
    state abandoned
    intent """
        Implement:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Implement:
            """
    }
}

record lege-ecosystem.work.milestone-5-9-7-kernel-optimization/1 : work {
    title "Milestone 5 — 9/7 kernel optimization"
    state abandoned
    intent """
        Implement:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Implement:
            """
    }
}

record lege-ecosystem.work.milestone-6-tier-1-mq-optimization/1 : work {
    title "Milestone 6 — Tier-1/MQ optimization"
    state abandoned
    intent """
        Implement:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Implement:
            """
    }
}

record lege-ecosystem.work.milestone-7-packed-renderer-output/1 : work {
    title "Milestone 7 — Packed renderer output"
    state abandoned
    intent """
        Implement:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Implement:
            """
    }
}

record lege-ecosystem.work.milestone-8-roi-decode/1 : work {
    title "Milestone 8 — ROI decode"
    state abandoned
    intent """
        Implement only after reduced-resolution decode is stable and measured.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Implement only after reduced-resolution decode is stable and measured.
            """
    }
}

record lege-ecosystem.work.mit-license-readme-about/1 : work {
    title "Present Lege as MIT-licensed and relabel About readme"
    state completed
    scope [
        path "Cargo.toml",
        path "lege-misc/docs/documentation.html",
        path "lege-misc/docs/licenses.html",
        path "lege-process/Cargo.toml",
        path "lege-process/GUI/Freya/src/app.rs",
        path "lege-process/GUI/Freya/src/backend.rs",
        path "lege-process/LICENSE",
        path "lege-process/README.md",
        path "lege-process/language_service/**/*.json",
        path "lege-process/packaging/appimage/com.legeapp.Lege.appdata.xml"
    ]
    intent """
        Replace stale AGPL presentation for Lege with MIT license material and metadata while retaining the separately distributed AGPL djvu-encoder notice, and make the compact About-page Readme button fit its neighbors.
        """
    acceptance {
        check mit-license-material {
            statement """
                Lege's shipped license text, crate metadata, AppStream metadata, README, and embedded notices identify MIT while leaving the separate djvu-encoder AGPL notice intact.
                """
            method manual
            verified_by [ @lege-ecosystem.evidence.mit-license-notice-audit/1 ]
        }
        check readme-about-ui {
            statement """
                The embedded About document tests pass with the Readme label and compact About action buttons.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.mit-license-readme-freya/1 ]
        }
    }
}

record lege-ecosystem.work.next-audit-phases-after-structural-dwt-openjpeg/1 : work {
    title "Next audit phases (after structural DWT + OpenJPEG baseline)"
    state abandoned
    intent """
        Prerequisite reading:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md"
        excerpt """
            Prerequisite reading:
            """
    }
}

record lege-ecosystem.work.objective/1 : work {
    title "1. Objective"
    state abandoned
    intent """
        Optimize jp2lam’s decoder for two related but distinct uses:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Optimize jp2lam’s decoder for two related but distinct uses:
            """
    }
}

record lege-ecosystem.work.ocr-backend-scheduler/1 : work {
    title "Implement backend routing and cross-document OCR scheduling"
    state superseded
    scope [
        path "lege-document-ocr/crates/pipeline/**",
        path "lege-gpu/**",
        path "lege-ocr/**"
    ]
    intent """
        Add backend capability APIs, Windows AI and legacy routing, Paddle v6 model adapters, bounded ROI batching, and compact recognition output.
        """
    acceptance {
        check backend-tests {
            statement """
                Backend and GPU OCR tests pass.
                """
            method command
            command "cargo test -p lege-ocr -p lege-gpu"
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    source {
        kind internal
        role origin
        document "dedicated-ocr-plan-2026-08"
        use """
            Adopted product plan.
            """
    }
}

record lege-ecosystem.work.ocr-backend-scheduler/2 : work {
    title "Implement backend routing and cross-document OCR scheduling"
    state superseded
    scope [
        path "lege-document-ocr/crates/pipeline/**",
        path "lege-gpu/**",
        path "lege-ocr/**"
    ]
    intent """
        Add backend capability APIs, Windows AI and legacy routing, Paddle v6 model adapters, bounded ROI batching, and compact recognition output.
        """
    note """
        Implemented backend-neutral batch APIs, legacy Windows Runtime routing, verified lazy model packs, per-page Paddle line batching, and bounded low-confidence retry. Windows AI/NPU, cross-document scheduling, session pooling, and compact GPU CTC readback remain required.
        """
    acceptance {
        check backend-tests {
            statement """
                Backend and GPU OCR tests pass.
                """
            method command
            command "cargo test -p lege-ocr -p lege-gpu"
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    supersedes [ @lege-ecosystem.work.ocr-backend-scheduler/1 ]
}

record lege-ecosystem.work.ocr-backend-scheduler/3 : work {
    title "Implement backend routing and cross-document OCR scheduling"
    state superseded
    scope [
        path "lege-document-ocr/cli/**",
        path "lege-document-ocr/crates/docir/**",
        path "lege-document-ocr/crates/export/**",
        path "lege-document-ocr/crates/pipeline/**",
        path "lege-gpu/**",
        path "lege-ocr/**"
    ]
    intent """
        Add backend capability APIs, Windows AI and legacy routing, Paddle v6 model adapters, bounded ROI batching, and compact recognition output.
        """
    note """
        Implemented bounded cross-document Paddle recognition scheduling, configurable concurrent CLI workers, GPU-side CTC top-two reduction with compact readback, checksum-pinned model-pack layout/table/formula adapters, and structured DocIR integration. Windows AI/NPU support is explicitly deferred until target hardware and deployment requirements are known. The existing performance corpus remains external until its location and provenance are linked. No specialist weights are bundled; model-pack graph parity and corpus qualification remain required before a new model becomes a default.
        """
    acceptance {
        check backend-tests {
            statement """
                The OCR backend and GPU test suites pass, including compact recognition parity.
                """
            method command
            command "cargo test -p lege-ocr -p lege-gpu"
            verified_by [ @lege-ecosystem.evidence.ocr-gpu-scheduler-specialists-2026-08-11/1 ]
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    supersedes [ @lege-ecosystem.work.ocr-backend-scheduler/2 ]
}

record lege-ecosystem.work.ocr-backend-scheduler/4 : work {
    title "Implement backend routing and cross-document OCR scheduling"
    state completed
    scope [ path "lege-document-ocr/**", path "lege-gpu/**", path "lege-ocr/**" ]
    intent """
        Add backend capability APIs, Windows AI and legacy routing, Paddle v6 model adapters, bounded ROI batching, and compact recognition output.
        """
    note """
        Implemented bounded cross-document Paddle recognition scheduling, configurable concurrent CLI workers, GPU-side CTC top-two reduction with compact readback, checksum-pinned model-pack layout/table/formula adapters, and structured DocIR integration. Windows AI/NPU support is explicitly deferred until target hardware and deployment requirements are known. The existing performance corpus remains external until its location and provenance are linked. No specialist weights are bundled; model-pack graph parity and corpus qualification remain required before a new model becomes a default.
        """
    acceptance {
        check backend-tests {
            statement """
                The OCR backend and GPU test suites pass, including compact recognition parity.
                """
            method command
            command "cargo test -p lege-ocr -p lege-gpu"
            verified_by [ @lege-ecosystem.evidence.ocr-gpu-scheduler-specialists-2026-08-11/1 ]
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    supersedes [ @lege-ecosystem.work.ocr-backend-scheduler/3 ]
}

record lege-ecosystem.work.ocr-correction-exports/1 : work {
    title "Implement correction and staged document exports"
    state superseded
    scope [
        path "lege-document-ocr/crates/export/**",
        path "lege-document-ocr/crates/pipeline/**"
    ]
    intent """
        Add evidence-preserving English OCR correction and production exporters for the approved staged formats.
        """
    acceptance {
        check export-tests {
            statement """
                Correction and exporter tests pass.
                """
            method command
            command "cargo test -p lege-document-export"
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    source {
        kind internal
        role origin
        document "dedicated-ocr-plan-2026-08"
        use """
            Adopted product plan.
            """
    }
}

record lege-ecosystem.work.ocr-correction-exports/2 : work {
    title "Implement correction and staged document exports"
    state superseded
    scope [
        path "lege-document-ocr/crates/export/**",
        path "lege-document-ocr/crates/pipeline/**"
    ]
    intent """
        Add evidence-preserving English OCR correction and production exporters for the approved staged formats.
        """
    note """
        Implemented conservative evidence-preserving correction and JSON, text, HTML, searchable PDF, DOCX, Markdown, ALTO, PAGE XML, hOCR, LaTeX, CSV, and XLSX exporters. External conformance hardening, specialist layout/table/formula models, and PDF/A remain required.
        """
    acceptance {
        check export-tests {
            statement """
                Correction and exporter tests pass.
                """
            method command
            command "cargo test -p lege-document-export"
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    supersedes [ @lege-ecosystem.work.ocr-correction-exports/1 ]
}

record lege-ecosystem.work.ocr-correction-exports/3 : work {
    title "Implement correction and staged document exports"
    state superseded
    scope [
        path "lege-document-ocr/crates/docir/**",
        path "lege-document-ocr/crates/export/**",
        path "lege-document-ocr/crates/pipeline/**"
    ]
    intent """
        Add evidence-preserving English OCR correction and production exporters for the approved staged formats.
        """
    note """
        Correction and staged exporters are implemented. Table and formula specialist results now flow through DocIR and existing exporters; formula regions preserve the raw OCR block, and correction/QA accounting traverses table cells and formula evidence. External specialist model qualification, incremental source-PDF text overlay, and validated PDF/A remain open production gates.
        """
    acceptance {
        check export-tests {
            statement """
                Correction and exporter tests pass.
                """
            method command
            command "cargo test -p lege-document-export"
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    supersedes [ @lege-ecosystem.work.ocr-correction-exports/2 ]
}

record lege-ecosystem.work.ocr-correction-exports/4 : work {
    title "Implement correction and staged document exports"
    state completed
    scope [ path "lege-document-ocr/**" ]
    intent """
        Add evidence-preserving English OCR correction and production exporters for the approved staged formats.
        """
    note """
        Correction and staged exporters are implemented. Table and formula specialist results now flow through DocIR and existing exporters; formula regions preserve the raw OCR block, and correction/QA accounting traverses table cells and formula evidence. External specialist model qualification, incremental source-PDF text overlay, and validated PDF/A remain open production gates.
        """
    acceptance {
        check export-tests {
            statement """
                Correction and exporter tests pass.
                """
            method command
            command "cargo test -p lege-document-export"
            verified_by [ @lege-ecosystem.evidence.document-export-tests-20260908/1 ]
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    supersedes [ @lege-ecosystem.work.ocr-correction-exports/3 ]
}

record lege-ecosystem.work.ocr-missing-space-correction/1 : work {
    title "Conservative missing-space OCR correction"
    state completed
    scope [
        path "lege-document-ocr/README.md",
        path "lege-document-ocr/cli/src/main.rs",
        path "lege-document-ocr/crates/pipeline/src/correction.rs",
        path "lege-document-ocr/crates/pipeline/src/lib.rs"
    ]
    intent """
        Apply high-confidence missing-space repairs without overwriting raw OCR, keep risky edit-distance spelling opt-in, and measure accuracy and throughput on the TensorRT sample.
        """
    acceptance {
        check correction-tests {
            statement """
                Pipeline and CLI correction behavior passes focused and integration tests.
                """
            method command
            command "$env:LEGE_TENSORRT_OCR_ROOT='Z:\\missing'; cargo test -p lege-document-pipeline -p lege-document-ocr-cli"
            verified_by [ @lege-ecosystem.evidence.ocr-missing-space-tests/1 ]
        }
        check export-tests {
            statement """
                Corrected/raw evidence remains exportable through the document export crate.
                """
            method command
            command "cargo test -p lege-document-export"
            verified_by [ @lege-ecosystem.evidence.ocr-missing-space-export-tests/1 ]
        }
        check sample-quality {
            statement """
                A ten-page TensorRT sample shows audited spacing repairs with no identified false split and no material steady-state throughput regression.
                """
            method observation
            verified_by [ @lege-ecosystem.evidence.ocr-missing-space-sample/1 ]
        }
    }
    derived_from [ @lege-ecosystem.work.ocr-correction-exports/4 ]
}

record lege-ecosystem.work.ocr-pdf-intake/1 : work {
    title "Add OCR-aware PDF intake and preservation"
    state superseded
    scope [ path "lege-pdf/read/**", path "lege-pdf/write/**" ]
    intent """
        Classify native, scanned, hybrid, and render-required pages; expose direct scan extraction and source-preserving searchable-PDF overlay behavior.
        """
    acceptance {
        check intake-tests {
            statement """
                PDF intake, classification, and writer tests pass.
                """
            method command
            command "cargo test -p lege-pdf-read -p lege-pdf-write"
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    source {
        kind internal
        role origin
        document "dedicated-ocr-plan-2026-08"
        use """
            Adopted product plan.
            """
    }
}

record lege-ecosystem.work.ocr-pdf-intake/2 : work {
    title "Add OCR-aware PDF intake and preservation"
    state completed
    scope [ path "lege-pdf/read/**", path "lege-pdf/write/**" ]
    intent """
        Classify native, scanned, hybrid, and render-required pages; expose direct scan extraction and source-preserving searchable-PDF overlay behavior.
        """
    note """
        Implemented page classification, one-pass semantic compilation, strict direct JPEG extraction, native-source preservation, and opt-in rasterized searchable PDF. A true incremental overlay for scanned source objects remains required.
        """
    acceptance {
        check intake-tests {
            statement """
                PDF intake, classification, and writer tests pass.
                """
            method command
            command "cargo test -p lege-pdf-read -p lege-pdf-write"
            verified_by [ @lege-ecosystem.evidence.pdf-intake-writer-tests-20260908/1 ]
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    supersedes [ @lege-ecosystem.work.ocr-pdf-intake/1 ]
}

record lege-ecosystem.work.ocr-product-foundation/1 : work {
    title "Create OCR product foundation"
    state superseded
    scope [ path "Cargo.toml", path "lege-document-ocr/**" ]
    intent """
        Add the grouped product crates, versioned DocIR, persistent batch contracts, CLI surface, and core exporter interfaces.
        """
    acceptance {
        check foundation-tests {
            statement """
                Foundation crate tests pass.
                """
            method command
            command "cargo test -p lege-docir -p lege-document-batch -p lege-document-export -p lege-document-ocr-cli"
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    source {
        kind internal
        role origin
        document "dedicated-ocr-plan-2026-08"
        use """
            Adopted product plan.
            """
    }
}

record lege-ecosystem.work.ocr-product-foundation/2 : work {
    title "Create OCR product foundation"
    state completed
    scope [ path "Cargo.toml", path "lege-document-ocr/**" ]
    intent """
        Add the grouped product crates, versioned DocIR, persistent batch contracts, CLI surface, and core exporter interfaces.
        """
    acceptance {
        check foundation-tests {
            statement """
                The DocIR, resumable batch, pipeline, exporters, CLI, PDF intake, and OCR integration tests pass.
                """
            method command
            command "cargo test -p lege-docir -p lege-document-batch -p lege-document-export -p lege-document-pipeline -p lege-document-ocr-cli -p lege-pdf-read -p lege-ocr --features lege-document-pipeline/paddle-ocr --lib --tests"
            verified_by [ @lege-ecosystem.evidence.ocr-foundation-tests-2026-08-11/1 ]
        }
        check production-lints {
            statement """
                The new OCR product library and CLI production targets pass clippy with warnings denied.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.ocr-foundation-clippy-cli-2026-08-11/1,
                @lege-ecosystem.evidence.ocr-foundation-clippy-libs-2026-08-11/1
            ]
        }
    }
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli/1 ]
    supersedes [ @lege-ecosystem.work.ocr-product-foundation/1 ]
}

record lege-ecosystem.work.office-printing/1 : work {
    title "Build lege-pdf-print: office printing for the Lege PDF stack"
    state completed
    scope [
        path "lege-pdf/agent/src/commands/print.rs",
        path "lege-pdf/print/**",
        path "lege-pdf/read/src/session.rs",
        path "lege-pdf/render/crates/pdf-document/src/pages.rs"
    ]
    intent """
        Implement phases 0-4 of lege-pdf/print/PLAN.md: page-box parsing in the renderer, a pure imposition core, banded sheet composition over lege-pdf-read, file/CUPS/winspool spooler backends, and a `lege-pdf print` frontend with a spooler-free --dry-run. Nothing in the ecosystem printed before this: a repo-wide search for cups, winspool, PrintDlg and StartDoc returned nothing.
        """
    note """
        Built on branch feat/printing by five parallel agents against a crate skeleton that fixed the shared vocabulary first (PrintOptions, PrintJob, Placement, Sheet, the Spooler trait, paper and margins), so the phases were written against a settled API rather than against each other.
        """
    claim banding {
        text """
            Sheets compose in horizontal bands rather than whole rasters: an A4 sheet at 1200 DPI in RGB is about 390 MB. Each placement is rendered once, cropped to the part that survives clipping, and dropped once the last band passes it.
            """
    }
    claim copies-belong-to-the-spooler {
        text """
            impose emits the sheet run for exactly one copy. expand_copies is a separate function that callers use only for the file backend and dry-run, because CUPS and winspool both take a native copy count and expanding as well would print copies squared pages.
            """
    }
    claim imposition-is-pure {
        text """
            layout.rs is pure geometry with no I/O, no OS and no renderer dependency: it is where a wrong-looking printout's bug would live, and the only part that can be tested exhaustively without a printer.
            """
    }
    claim page-boxes {
        text """
            pdf-document parses /BleedBox, /TrimBox and /ArtBox from the page's own dictionary only, because ISO 32000-1 Table 29 makes exactly Resources, MediaBox, CropBox and Rotate inheritable. Each is clamped to the MediaBox and falls back to the CropBox, so a document carrying no prepress boxes behaves identically whichever box a print job asks for.
            """
    }
    claim pass-through-gate {
        text """
            A job takes the pass-through route -- the original PDF bytes spooled unmodified -- only when PrintOptions::is_pass_through_capable() and DeviceCapabilities::accepts_pdf both hold. Windows reports accepts_pdf false and rejects a PDF payload, so gating on the options alone would fail there instead of composing.
            """
    }
    claim print-is-not-an-mcp-tool {
        text """
            The print subcommand is deliberately not exposed as an MCP tool. Spooling actuates hardware, which no bounded read contract should hand a model, and a planning-only tool would be a strictly weaker duplicate of --dry-run, which an agent can already shell out to safely.
            """
    }
}

record lege-ecosystem.work.open-source-monorepo-relaunch/1 : work {
    title "Relaunch Lege as the canonical open-source codec monorepo"
    state proposed
    scope [
        path ".gitignore",
        path "Cargo.toml",
        path "README.md",
        path "lege-codecs/**",
        path "lege-document-ocr/**",
        path "lege-process/**",
        path "to-do-plans/**"
    ]
    intent """
        Publish one sanitized Lege ecosystem source repository, restore Lege's direct DJVULibRust API integration without rolling back later behavior, retire duplicate codec mirrors, and provide checksum-pinned downloads for excluded Document OCR models.
        """
    note """
        Plan of record is to-do-plans/open-source-monorepo-relaunch.md. Licensing of the AGPL DJVULibRust/MIT Lege combined work and the rewritten public-history cutover are explicit blocking decisions.
        """
    claim asset-policy {
        text """
            The four required embedded Lege ONNX models remain tracked, while optional large Document OCR/TurboOCR model tiers are fetched atomically from an immutable checksum-pinned manifest.
            """
    }
    claim canonical-codecs {
        text """
            The canonical codec source will be lege-codecs/djvulibrust, lege-codecs/jbig2enc-rust, and lege-codecs/jp2lam in LegeApp/Lege; old repositories will preserve history as archived signposts rather than mirrors.
            """
    }
    claim historical-djvu-seam {
        text """
            The reachable history's last direct DJVULibRust integration is Lege v1.4.61 at b47958d; the subprocess boundary begins at v1.4.64 commit 8382568, and no exact v1.4.72 revision is reachable.
            """
    }
    acceptance {
        check canonical-codec-source {
            statement """
                All three codec repositories identify LegeApp/Lege as canonical and no workflow mirrors codec source out of the monorepo.
                """
            method observation
        }
        check integrated-djvu-api {
            statement """
                Lege's current DjVu modes pass without spawning or locating the standalone djvu-encoder helper.
                """
            method command
        }
        check license-consistency {
            statement """
                Root, application, and crate license declarations and included texts are consistent with the selected direct-linking license decision.
                """
            method observation
        }
        check public-cutover {
            statement """
                The public Lege repository passes clean-clone Linux and Windows smoke tests and the old codec repositories are archived with migration notices and preserved history.
                """
            method manual
        }
        check sanitized-public-tree {
            statement """
                A fresh publication clone passes secret scanning and contains no unapproved tracked or historical large development artifacts.
                """
            method command
        }
        check verified-model-bootstrap {
            statement """
                A clean clone downloads and verifies required Document OCR model assets atomically from the pinned manifest, while embedded runtime models remain allowlisted and intact.
                """
            method command
        }
    }
    source {
        kind external
        role rationale
        url "https://www.gnu.org/licenses/agpl-3.0.html"
        use """
            Primary license text for the blocking combined-work licensing review.
            """
    }
    source {
        kind internal
        role origin
        path "to-do-plans/open-source-monorepo-relaunch.md"
        use """
            Implementation plan, established inventory, decisions, gates, and cutover sequence.
            """
    }
}

record lege-ecosystem.work.optimize-flaggrid/1 : work {
    title "23. Optimize `FlagGrid`"
    state abandoned
    intent """
        The implementing agent needs the actual `FlagGrid` implementation before choosing changes. Measure:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The implementing agent needs the actual `FlagGrid` implementation before choosing changes. Measure:
            """
    }
}

record lege-ecosystem.work.optimize-mqdecoder-only-after-direct-attribution/1 : work {
    title "24. Optimize `MqDecoder` only after direct attribution"
    state abandoned
    intent """
        The MQ arithmetic state is inherently serial within one codeword segment. SIMD is unlikely to help materially inside one MQ stream.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The MQ arithmetic state is inherently serial within one codeword segment. SIMD is unlikely to help materially inside one MQ stream.
            """
    }
}

record lege-ecosystem.work.package-primary-tensorrt-ocr-runtime/1 : work {
    title "Package TensorRT as the primary Windows OCR runtime"
    state completed
    scope [
        path "lege-document-ocr/README.md",
        path "lege-document-ocr/cli/src/main.rs",
        path "lege-document-ocr/crates/pipeline/src/lib.rs",
        path "lege-document-ocr/scripts/**",
        path "lege-document-ocr/turboocr/CMakeLists.txt",
        path "lege-document-ocr/turboocr/scripts/build_windows_trt.ps1",
        path "lege-ocr/src/engine_tensorrt.rs"
    ]
    intent """
        Make the Windows OCR distribution self-discover its packaged TensorRT worker, models, and native runtime; require TensorRT on NVIDIA systems; reserve WinOCR for machines without an NVIDIA driver; provide a portable GPU build and an installer-ready staging/preflight workflow.
        """
    claim packaged-layout {
        text """
            The release layout keeps the CLI beside a self-contained tensorrt directory containing the worker, models, and runtime DLLs, and is discoverable without environment variables.
            """
    }
    claim portable-worker {
        text """
            The published CUDA worker includes forward-compatible PTX so it is not limited to the build machine's Ada compute capability.
            """
    }
    claim primary-routing {
        text """
            On Windows, automatic backend selection must fail closed when an NVIDIA driver is present and TensorRT is missing or fails preflight; WinOCR is only the no-NVIDIA fallback.
            """
    }
    acceptance {
        check installer-staging {
            statement """
                The staging script produces an installer-ready payload including cold engine-build dependencies and the packaged CLI doctor passes against it.
                """
            method command
            command "powershell -File lege-document-ocr/scripts/package_windows_tensorrt.ps1 ..."
            verified_by [
                @lege-ecosystem.evidence.sdk-isolated-cold-tensorrt-package-smoke-20260815/1
            ]
        }
        check packaged-discovery {
            statement """
                Tests prove the installed tensorrt/bin, tensorrt/models, and tensorrt/runtime layout is discovered with its DLL directory.
                """
            method command
            command "cargo test -p lege-ocr engine_tensorrt"
            verified_by [
                @lege-ecosystem.evidence.packaged-tensorrt-layout-discovery-20260815/1
            ]
        }
        check portable-build {
            statement """
                The production worker build defaults to a CUDA architecture set containing forward-compatible PTX rather than Ada-only sm_89.
                """
            method observation
            verified_by [ @lege-ecosystem.evidence.portable-tensorrt-worker-images-20260815/1 ]
        }
        check primary-routing {
            statement """
                Automated tests prove Auto uses WinOCR only when no NVIDIA driver is present and returns an initialization error for missing or broken TensorRT on an NVIDIA system.
                """
            method command
            command "cargo test -p lege-document-pipeline"
            verified_by [ @lege-ecosystem.evidence.primary-tensorrt-routing-tests-20260815/1 ]
        }
    }
    derived_from [
        @lege-ecosystem.assessment.document-ocr-installer-readiness-20260815,
        @lege-ecosystem.work.windows-tensorrt-document-ocr-integration
    ]
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli ]
}

record lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec/1 : work {
    title "PDF renderer follow-ups (outside pure codec)"
    state proposed
    intent """
        1. Keep using session `Jp2Decoder` + packed Rgb8 (already done)
        2. When GPU compositing wants 4-byte pixels: map `DecodeOutputFormat::Bgra8`/`Rgbx8` through a new `DecodedFormat` and skip RGB→BGRA swizzle
        3. Optional decode telemetry line (`JP2LAM_DECODE_TRACE`) for one-shot integration diagnosis
        4. Re-run pdfium-diff on a JPX-heavy page after Phase 3
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md"
        excerpt """
            1. Keep using session `Jp2Decoder` + packed Rgb8 (already done)
            2. When GPU compositing wants 4-byte pixels: map `DecodeOutputFormat::Bgra8`/`Rgbx8` through a new `DecodedFormat` and skip RGB→BGRA swizzle
            3. Optional decode telemetry line (`JP2LAM_DECODE_TRACE`) for one-shot integration diagnosis
            4. Re-run pdfium-diff on a JPX-heavy page after Phase 3
            """
    }
}

record lege-ecosystem.work.performance-scoreboard/1 : work {
    title "36. Performance scoreboard"
    state abandoned
    intent """
        Record, per image:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Record, per image:
            """
    }
}

record lege-ecosystem.work.permanent-decoder-corpus/1 : work {
    title "33. Permanent decoder corpus"
    state abandoned
    intent """
        Create a jp2lam-specific corpus containing:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Create a jp2lam-specific corpus containing:
            """
    }
}

record lege-ecosystem.work.phase-3-fused-packed-finalization-done-2026-08/1 : work {
    title "Phase 3 — Fused packed finalization — DONE (2026-08-05)"
    state abandoned
    intent """
        **Landed:**
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md"
        excerpt """
            **Landed:**
            """
    }
}

record lege-ecosystem.work.phase-4-tier-1-tier-2-refinement/1 : work {
    title "Phase 4 — Tier-1 / Tier-2 refinement"
    state abandoned
    intent """
        **Goal:** better core utilization once DWT memory traffic is no longer dominant.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md"
        excerpt """
            **Goal:** better core utilization once DWT memory traffic is no longer dominant.
            """
    }
}

record lege-ecosystem.work.phase-5-true-windowed-line-based-roi/1 : work {
    title "Phase 5 — True windowed / line-based ROI"
    state abandoned
    intent """
        **Goal:** single-tile ROI cost proportional to ROI + filter halo, not full tile.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md"
        excerpt """
            **Goal:** single-tile ROI cost proportional to ROI + filter halo, not full tile.
            """
    }
}

record lege-ecosystem.work.phase-6-isa-pgo-optional-gpu/1 : work {
    title "Phase 6 — ISA / PGO / optional GPU"
    state abandoned
    intent """
        Only after Phases 3–4:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/NEXT-audit-phases.md"
        excerpt """
            Only after Phases 3–4:
            """
    }
}

record lege-ecosystem.work.post-precinct-census-2026-07-19-from-pdf/1 : work {
    title "7. Post-precinct census (2026-07-19, from pdf-renderer) — new residuals"
    state abandoned
    intent """
        After the precinct/SOP/EPH + degenerate-DWT round landed, a fresh full-corpus
        `jpxcensus` (451 blank docs, 1224 JPX streams) decodes **1207 (98.6%)**. The 17
        residuals, for this session:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
        excerpt """
            After the precinct/SOP/EPH + degenerate-DWT round landed, a fresh full-corpus
            `jpxcensus` (451 blank docs, 1224 JPX streams) decodes **1207 (98.6%)**. The 17
            residuals, for this session:
            """
    }
}

record lege-ecosystem.work.prefer-discrete-gpu/1 : work {
    title "Prefer discrete GPU for compute and presentation"
    state superseded
    scope [
        path "lege-gpu/src/presentation.rs",
        path "lege-gpu/src/vision/runtime/device.rs",
        path "lege-gpu/src/wgpu_setup.rs"
    ]
    intent """
        Make automatic wgpu adapter selection deterministic by trying discrete adapters before integrated, virtual, and CPU adapters for both shared compute/layout detection and surface presentation, while retaining explicit adapter overrides and graceful fallback.
        """
    acceptance {
        check compute-hardware {
            statement """
                On a host with an enumerated discrete GPU, the shared compute context selects a discrete adapter.
                """
            method command
            command "LEGE_TEST_GPU_SELECTION=1 cargo test -p lege-gpu --features layout-detection shared_context_prefers_discrete_when_available --lib -- --nocapture"
            verified_by [ @lege-ecosystem.evidence.discrete-auto-selection-20260813/1 ]
        }
        check presentation-build {
            statement """
                The presentation feature compiles after using the same ordered fallback policy.
                """
            method command
            command "cargo check -p lege-gpu --features presentation,layout-detection"
            verified_by [ @lege-ecosystem.evidence.presentation-layout-build-20260813/1 ]
        }
        check selection-policy {
            statement """
                Unit tests prove discrete adapters rank ahead of integrated and software adapters.
                """
            method command
            command "cargo test -p lege-gpu wgpu_setup::tests --lib"
            verified_by [ @lege-ecosystem.evidence.adapter-order-tests-20260813/1 ]
        }
    }
}

record lege-ecosystem.work.prefer-discrete-gpu/2 : work {
    title "Prefer discrete GPU for compute and presentation"
    state completed
    scope [
        path "lege-gpu/src/presentation.rs",
        path "lege-gpu/src/vision/runtime/device.rs",
        path "lege-gpu/src/wgpu_setup.rs"
    ]
    intent """
        Make automatic wgpu adapter selection deterministic by trying discrete adapters before integrated, virtual, and CPU adapters for both shared compute/layout detection and surface presentation, while retaining explicit adapter overrides and graceful fallback.
        """
    acceptance {
        check compute-hardware {
            statement """
                On a host with an enumerated discrete GPU, the shared compute context selects a discrete adapter.
                """
            method command
            command "LEGE_TEST_GPU_SELECTION=1 cargo test -p lege-gpu --features layout-detection shared_context_prefers_discrete_when_available --lib -- --nocapture"
            verified_by [ @lege-ecosystem.evidence.discrete-auto-selection-20260813/2 ]
        }
        check presentation-build {
            statement """
                The presentation feature compiles after using the same ordered fallback policy.
                """
            method command
            command "cargo check -p lege-gpu --features presentation,layout-detection"
            verified_by [ @lege-ecosystem.evidence.presentation-layout-build-20260813/2 ]
        }
        check selection-policy {
            statement """
                Unit tests prove discrete adapters rank ahead of integrated and software adapters.
                """
            method command
            command "cargo test -p lege-gpu wgpu_setup::tests --lib"
            verified_by [ @lege-ecosystem.evidence.adapter-order-tests-20260813/2 ]
        }
    }
    supersedes [ @lege-ecosystem.work.prefer-discrete-gpu/1 ]
}

record lege-ecosystem.work.prepared-onnx-shared-gpu-session/1 : work {
    title "Expose prepared ONNX sessions on the shared GPU executor"
    state completed
    intent """
        Expose a public OnnxSession GPU execution method for prepared scene_image ONNX
        graphs. Build each single-flight compiled graph on Lege's process-wide shared
        wgpu device, retain the deterministic CPU reference method, and verify a real
        consumer executes prepared scene graphs on the Linux RTX 4060 Vulkan adapter.
        """
    acceptance {
        check linux-4060-consumer {
            statement """
                A real raw-autotune consumer run executes both prepared scene graphs on the
                Linux RTX 4060 Vulkan adapter without CPU fallback.
                """
            method command
            command "cd ../raw-autotune && WGPU_ADAPTER_NAME=4060 WGPU_REQUIRE_REAL_GPU=1 cargo run --release -- raw/raw_3rd_batch/_DSC1289.ARW --output .agent/scratch/scene-gpu-4060-20260813 --format png --sidecar --no-summary --overwrite --jobs 1 --semantic"
            verified_by [ @lege-ecosystem.evidence.raw-scene-rtx4060-20260813/1 ]
        }
        check session-build {
            statement """
                lege-gpu release-checks with the public prepared-session GPU method.
                """
            method command
            command "cargo check -p lege-gpu --release"
            verified_by [ @lege-ecosystem.evidence.prepared-onnx-session-build-20260813/1 ]
        }
    }
}

record lege-ecosystem.work.preservation-mode/1 : work {
    title "Preservation mode: an MRC document beside the e-ink preparation mode"
    state proposed
    scope [
        path "lege-codecs/djvulibrust/**",
        path "lege-process/GUI/Freya/**",
        path "lege-process/PRESERVATION-MODE-PLAN.md",
        path "lege-process/core/clean_gray.rs",
        path "lege-process/core/djvu.rs",
        path "lege-process/pipeline/config.rs",
        path "lege-process/pipeline/pdf_tokio_pipeline.rs"
    ]
    intent """
        Give Lege a second top-level mode. Preparation, everything it does today, rebuilds a book for an e-ink screen: binarize, dither, drop the colour, re-encode at the device's height. Preservation would keep the book as it is — no binarization of the page, no colour touched — and compress it as layers instead: a JPEG 2000 background under a crisp JBIG2 ink layer (or the DjVu equivalent, IW44 under JB2), at a requested resolution, with hOCR still running and truetyping available as an opt-in. The plan is written; nothing is implemented.
        """
    note """
        The plan is `lege-process/PRESERVATION-MODE-PLAN.md` (2026-09-03). The load-bearing finding is that the one path that already preserves a page's own colour and ink, `encode_preserved_mrc_base_layer`, is reachable only when the input PDF is *already* MRC: `qualifying_jbig2_smask` is a strict structural probe over the compiled page, and nothing synthesizes its shape for an ordinary scan. Every other route converts to luma and normalizes contrast in `clean_page_for_mrc` before thresholding. So the mode cannot be built before the encoder it needs, which is why this is a plan and not a branch. Order: (1) an ink coverage plane from a fresh render, by supersampling the Sauvola mask into the existing exact-area `coverage_spans`; (2) colour through the MRC encoder — three-channel `mrc_background_with_coverage`, RGB JP2 and RGB IW44 backgrounds; (3) `PageMode::Preserved` with its own region policies and a CLI switch; (4) DjVu parity, a full-page colour background under the JB2 layer, and the `with_shared_dict` the encoder exposes and Lege never calls; (5) the GUI preparation/preservation toggle, first control in the left column with one sentence of tooltip each; (6) ink colour, which is what makes the result correct rather than merely faithful in shape. Not to be confused with `lege-document-ocr/PRESERVE-MODE-PLAN.md`, which is the business OCR CLI keeping a source PDF's own page images byte for byte.
        """
    claim coverage-has-no-producer {
        text """
            `mrc_background_with_coverage` consumes a continuous 0-255 ink coverage plane, but the only producer is the decoded source /SMask; binarization emits bilevel, so a preservation mode needs a coverage plane derived from a fresh render (supersampled Sauvola through `coverage_spans`).
            """
    }
    claim hocr-is-already-independent {
        text """
            On the default PP-OCR backend the recognizer reads the RGB raster rather than the bilevel mask and the text reaches the writer on its own channel, so hOCR carries into preservation mode unchanged.
            """
    }
    claim preserved-path-needs-an-mrc-input {
        text """
            The colour-preserving MRC path fires only when the input PDF is already MRC: `RenderSession::preserved_jbig2_smask` returns None unless the page is one full-page image whose /SMask is itself JBIG2, and no code synthesizes that shape for a plain scan.
            """
    }
    claim the-writers-already-layer {
        text """
            Neither writer blocks the mode: `lege-pdf-write` already stacks a JPX background (RGB or grey) under a JBIG2 /ImageMask stencil, and djvulibrust's LayerData already models Background(Pixmap) with Foreground/Mask(Bitmap). What is missing is a colour-capable segmentation, not a container.
            """
    }
    derived_from [ @lege-ecosystem.work.glyph-font-text-output ]
}

record lege-ecosystem.work.preserve-jbig2-smask-text-plane/1 : work {
    title "Preserve native JBIG2 image SMask text planes through PDF processing"
    state completed
    scope [
        path "lege-pdf/read/**",
        path "lege-pdf/render/crates/pdf-content/**",
        path "lege-pdf/render/crates/pdf-page-ir/**",
        path "lege-pdf/render/crates/pdf-render-cpu/**",
        path "lege-pdf/write/**",
        path "lege-process/core/**",
        path "lege-process/pipeline/**"
    ]
    intent """
        Add a conservative PDF-only quality-preservation path for scanned pages whose full-page image foreground carries a one-bit JBIG2 /SMask. Preserve the source JBIG2 page stream, globals, dimensions, decode polarity, and placement for output rather than re-thresholding rendered pixels. Separately decode the mask once and produce a Lanczos3-resized Gray8 ink-coverage plane for target-resolution background cleanup, OCR, layout alignment, and figure compositing; never re-encode that working plane as the authoritative text layer.
        """
    note """
        Initial scope is non-reflow PDF MRC/grayscale output. Eligibility is per page and requires an image /SMask (not /Mask, /ImageMask, page soft mask, or SMaskInData), codec JBIG2, one bit per component, intact page/global streams, binary-representable /Decode polarity, a single unambiguous axis-aligned full-page placement, scan-like image structure, and no unsupported paint whose preservation would depend on the foreground image. Mixed or ineligible pages use the existing full-render/binarize path. The native mask XObject may be scaled by PDF placement without resampling its stored pixels; rectangular crop and richer transforms are follow-ups. Reflow, deskew, DjVu, EPUB, arbitrary soft masks, multiple mask mosaics, and damaged JBIG2 are out of the first implementation.
        """
    claim authoritative-plane {
        text """
            The source JBIG2 stream and globals are the authoritative final text plane and remain byte-identical; the Lanczos3 Gray8 plane is a derived working surface only.
            """
    }
    claim first-scope-boundary {
        text """
            The first implementation targets non-reflow PDF grayscale/MRC output; transformations that require destructive mask raster rewriting remain out of scope.
            """
    }
    claim no-rethreshold {
        text """
            Eligible pages bypass scan-derived text binarization; continuous Lanczos3 coverage used by cleanup and OCR is not thresholded back into the final text layer.
            """
    }
    claim per-page-fallback {
        text """
            Failure to recognize, decode, place, or validate the preserved mask falls back per page to the existing renderer and processing pipeline without content loss.
            """
    }
    claim strict-eligibility {
        text """
            Automatic use is restricted to structurally verified one-bit JBIG2 image /SMask scan pages with representable polarity and full-page placement.
            """
    }
    acceptance {
        check eligibility-corpus {
            statement """
                Focused fixtures accept a full-page one-bit JBIG2 image /SMask with globals and reject maskless JPEG, non-JBIG2 SMask, /Mask, /ImageMask, SMaskInData, arbitrary grayscale SMask, ambiguous multiple masks, corrupt JBIG2, and unsupported placement.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.preserved-jbig2-smask-focused-tests/1 ]
        }
        check lanczos-working-plane {
            statement """
                The renderer/read seam decodes only the qualifying SMask at native dimensions and produces exactly one Lanczos3-resized Gray8 ink-coverage plane at the requested output geometry without invoking scan thresholding.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.preserved-jbig2-smask-focused-tests/1 ]
        }
        check mixed-page-fallback {
            statement """
                A mixed document processes eligible and ineligible pages in one run, preserving masks only where qualified and producing nonblank fallback pages elsewhere.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.preserved-jbig2-mixed-page-fallback/1 ]
        }
        check payload-roundtrip {
            statement """
                For eligible fixtures, the output JBIG2 page stream and globals are byte-identical to the input SMask payloads, and output stencil polarity renders black ink exactly where the source SMask opacity is nonzero.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.structures-jbig2-payload-roundtrip-corrected/1
            ]
        }
        check pipeline-bypass {
            statement """
                Eligible PDF MRC pages use the preserved mask for cleanup, OCR input, and final text composition while bypassing binarize_image and clean-page mask derivation; detected figure crops remain composited from the normal rendered page.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.structures-layout-preserved-mask-composition/1
            ]
        }
        check quality-oracle {
            statement """
                Rendering the output at the source mask grid yields the same ink support and connected-component topology as the decoded source SMask outside explicitly overlaid figure regions.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.structures-jbig2-ink-support-oracle/1 ]
        }
        check scope-guards {
            statement """
                Reflow, DjVu, EPUB, unsupported crop/deskew transforms, and non-PDF inputs cannot enter the preserved-mask path.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.preserved-jbig2-scope-and-bypass-audit/1 ]
        }
    }
    source {
        kind external
        role example
        url "https://github.com/koreader/koreader-base/pull/2361"
        use """
            Demonstrates that rendering only attached image masks is practical, while this work retains the mask as an output-quality source plane.
            """
    }
    source {
        kind external
        role rationale
        url "https://github.com/koreader/koreader/issues/7992"
        use """
            Documents the Internet Archive JPX plus JBIG2 SMask scan family and the benefit of isolating its pre-existing text mask.
            """
    }
    source {
        kind internal
        role rationale
        path "lege-pdf/render/crates/pdf-content/src/interpret.rs"
        use """
            The interpreter deliberately preserves codec-encoded JBIG2 SMask data instead of decoding it.
            """
    }
    source {
        kind internal
        role rationale
        path "lege-pdf/render/crates/pdf-page-ir/src/lib.rs"
        use """
            ImageSMask already retains native dimensions, decode metadata, codec identity, encoded payload, globals and codec parameters.
            """
    }
    source {
        kind internal
        role rationale
        path "lege-pdf/write/src/images.rs"
        use """
            The writer already emits JBIG2 streams with optional globals and ImageMask semantics but needs preserved polarity metadata.
            """
    }
    source {
        kind internal
        role rationale
        path "lege-process/pipeline/pdf_tokio_pipeline.rs"
        use """
            The current MRC path derives a new mask from rendered pixels and then composes a JP2 background with a JBIG2 mask.
            """
    }
}

record lege-ecosystem.work.produce-packed-output-directly/1 : work {
    title "12. Produce packed output directly"
    state abandoned
    intent """
        The PDF renderer ultimately needs 8-bit output. It should not receive three or four planar `Vec<i32>` components only to scale and interleave them later.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The PDF renderer ultimately needs 8-bit output. It should not receive three or four planar `Vec<i32>` components only to scale and interleave them later.
            """
    }
}

record lege-ecosystem.work.public-monorepo-reupload/1 : work {
    title "Prepare the public Lege monorepo reupload"
    state superseded
    intent """
        Prepare LegeApp/Lege for the approved public force cutover, with canonical in-tree codecs, redirect-only sister repositories, and release-ready metadata.
        """
    claim goal {
        text """
            LegeApp/Lege is the only maintained source repository; the three codec subtrees are canonical and their standalone repositories become redirect-only archives after the public force cutover.
            """
    }
    claim release {
        text """
            The public application release is 1.4.76 and jbig2enc-rust is prepared for a 0.5.4 crates.io release from the monorepo.
            """
    }
}

record lege-ecosystem.work.public-monorepo-reupload/2 : work {
    title "Prepare the public Lege monorepo reupload"
    state completed
    intent """
        Prepare LegeApp/Lege for the approved public force cutover, with canonical in-tree codecs, redirect-only sister repositories, and release-ready metadata.
        """
    claim release {
        text """
            The reupload preparation is done and the release numbers have since moved on: the
            application was 1.4.76 and jbig2enc-rust 0.5.4 when this record was written, and at
            2026-09-08 they are 1.5.1 (lege-process/Cargo.toml) and 0.6.0
            (lege-codecs/jbig2enc-rust/Cargo.toml). The repository-URL migration to
            github.com/liminalism/lege landed in 3afb72a4 across Cargo.toml, the READMEs, the
            AppStream metadata and the cutover script; AGPL-3.0 LICENSE and the per-subtree NOTICE
            are at the repo root; and README's canonical-codec-source section states the in-tree
            codec policy. The remaining public-cutover verification (clean-clone Linux and Windows
            smoke tests, secret scanning of the publication clone, pinned model bootstrap) is not
            part of this record; it is tracked by lege-ecosystem.work.open-source-monorepo-relaunch.
            """
    }
    supersedes [ @lege-ecosystem.work.public-monorepo-reupload/1 ]
}

record lege-ecosystem.work.quick-reference/1 : work {
    title "6. Quick reference"
    state abandoned
    intent """
        | Class | ~N | Error signature (substring) | Reject site | Repro obj |
        |---|---:|---|---|---|
        | Precincts/SOP/EPH | 10 | `precinct partitioning, SOP markers` | `decode_markers.rs:~475` | Armenia obj8 |
        | Degenerate DWT | 8 | `decomposition levels … exceed DWT limit` | `decode_markers.rs:~462` + `dwt/` | Neo-Confucianism obj9 |
        | Zero-length contribution | 6 | `zero byte length` | `t2.rs:~481` | (dump via census) |
        | TPsot==TNsot tail | 2 | `data after the tile's packet sequence` | `t2.rs:~171` + `mod.rs` assembly | Prussian obj15 |
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
        excerpt """
            | Class | ~N | Error signature (substring) | Reject site | Repro obj |
            |---|---:|---|---|---|
            | Precincts/SOP/EPH | 10 | `precinct partitioning, SOP markers` | `decode_markers.rs:~475` | Armenia obj8 |
            | Degenerate DWT | 8 | `decomposition levels … exceed DWT limit` | `decode_markers.rs:~462` + `dwt/` | Neo-Confucianism obj9 |
            | Zero-length contribution | 6 | `zero byte length` | `t2.rs:~481` | (dump via census) |
            | TPsot==TNsot tail | 2 | `data after the tile's packet sequence` | `t2.rs:~171` + `mod.rs` assembly | Prussian obj15 |
            """
    }
}

record lege-ecosystem.work.reduced-resolution-correctness/1 : work {
    title "35. Reduced-resolution correctness"
    state abandoned
    intent """
        Compare against an independent decoder invoked at the same JPEG 2000 reduction level.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Compare against an independent decoder invoked at the same JPEG 2000 reduction level.
            """
    }
}

record lege-ecosystem.work.remove-redundant-state-and-traversals/1 : work {
    title "21. Remove redundant state and traversals"
    state abandoned
    intent """
        Validate the following experimentally:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Validate the following experimentally:
            """
    }
}

record lege-ecosystem.work.renderer-efficiency-deferrals/1 : work {
    title "Disposition the deferred renderer efficiency findings"
    state proposed
    intent """
        Decide each renderer efficiency finding that the 2026-08-24 review identified but deliberately did not apply, so the reasoning is not lost and each can be picked up or declined on its own evidence.

        Provenance: a three-reviewer efficiency pass over lege-pdf/render at 7277babb. Eleven findings were applied and landed; these six were held back because each needs measurement, a dependency decision, or a workload fact that static reading could not supply. None of them is known to be wrong — they are unfunded, not rejected.

        Note the shared precondition: the in-tree synthetic rasterizer bench (crates/pdf-render-cpu/benches/raster.rs) draws only vector rects and shows roughly 8-10 percent run-to-run spread, so it can neither confirm nor refute any of these. Anything here that claims a speedup needs the real-PDF corpus (corpus/perf/pages.tsv) or tools/pdfium-diff timing, not that bench.
        """
    acceptance {
        check calrgb-adaptation-matrix-cache {
            statement """
                lege-pdf/render/crates/pdf-color/src/cie.rs xyz_to_srgb_white_point rebuilds the full white-point adaptation matrix (two 3x3 inversions) on every call, though only the final transform depends on the sample. Dispositioned when the build is split from the application and cached per CalRGB colour space, or when the per-operator (not per-pixel) call rate is shown to make it not worth the API change.
                """
            method manual
        }
        check ccitt-bulk-skip {
            statement """
                lege-pdf/render/crates/pdf-image/src/ccitt.rs find_bit: PDFium's 8-byte all-same bulk skip is omitted, and the file's own comment records that as a conscious simplification with identical output. Dispositioned when either the skip is restored with a measured decode-time win on real Group 3/4 scans, or the omission is confirmed deliberate and the comment says so explicitly.
                """
            method manual
        }
        check cmyk-source-conversion-cache {
            statement """
                lege-pdf/render/crates/pdf-render-cpu/src/image.rs cmyk_source_as_rgb8 recomputes the whole source conversion per draw with no cache. Dispositioned when the workload question is answered with evidence: does a single page ever paint the same CMYK image more than once (tiling-pattern cell or repeated XObject Do)? Cache keyed on the samples Arc pointer only if it does.
                """
            method manual
        }
        check lexer-name-bulk-scan {
            statement """
                lege-pdf/render/crates/pdf-syntax/src/lexer.rs read_name builds every /Name byte-by-byte via push, where next_token's regular-run branch already scans then slices once. Names are the hottest token kind in a PDF. Dispositioned when the bulk-scan-then-slice form lands with a lexer benchmark showing the win, or the micro-optimisation is declined as not worth the escape-path branch.
                """
            method manual
        }
        check postprocess-row-parallelism {
            statement """
                lege-pdf/render/crates/pdf-postprocess/src/cpu.rs resize and sauvola_map are row-independent but single-threaded, and the crate has no rayon dependency. Dispositioned when either rayon is adopted for those passes with a measured win, or the decision is recorded that CpuPostprocess stays single-threaded and why. FloydSteinberg dither is out of scope: its error diffusion is a genuine row-to-row dependency.
                """
            method manual
        }
        check transform-rect-shared-helper {
            statement """
                The transformed-rect bounding box is implemented twice: lege-pdf/render/crates/pdf-text/src/lib.rs transform_rect and lege-pdf/render/crates/pdf-content/src/lower.rs include_transformed_rect, in two crates that both already depend on pdf-geom. Dispositioned when a single Matrix::transform_rect lands in pdf-geom and both call it, or the duplication is accepted with a reason.
                """
            method manual
        }
    }
    author "GitHub Uploader"
}

record lege-ecosystem.work.replace-per-code-block-result-allocation/1 : work {
    title "10. Replace per-code-block result allocation"
    state abandoned
    intent """
        The Tier-1 implementation currently allocates:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The Tier-1 implementation currently allocates:
            """
    }
}

record lege-ecosystem.work.replace-the-merged-block-hashmap/1 : work {
    title "30. Replace the merged-block `HashMap`"
    state abandoned
    intent """
        The full band and code-block geometry is known when `TilePacketDecoder` is constructed.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The full band and code-block geometry is known when `TilePacketDecoder` is constructed.
            """
    }
}

record lege-ecosystem.work.replace-the-single-fixed-output-entry-point/1 : work {
    title "5. Replace the single fixed-output entry point internally"
    state abandoned
    intent """
        Keep:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Keep:
            """
    }
}

record lege-ecosystem.work.required-profiles/1 : work {
    title "4. Required profiles"
    state abandoned
    intent """
        Capture separate flamegraphs and hardware counters for:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Capture separate flamegraphs and hardware counters for:
            """
    }
}

record lege-ecosystem.work.resolution-aware-renderer-path/1 : work {
    title "Resolution-aware renderer path"
    state abandoned
    intent """
        * JPX page compiled total below 150–200 ms initially.
        * Longer-term target near 100 ms when one-level reduction is available.
        * MRC compiled total below 180–220 ms.
        * No material deterioration in PDFium differential severity.
        * No whole-document throughput regression.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            * JPX page compiled total below 150–200 ms initially.
            * Longer-term target near 100 ms when one-level reduction is available.
            * MRC compiled total below 180–220 ms.
            * No material deterioration in PDFium differential severity.
            * No whole-document throughput regression.
            """
    }
}

record lege-ecosystem.work.resolution-selection-in-the-pdf-renderer/1 : work {
    title "6. Resolution selection in the PDF renderer"
    state abandoned
    intent """
        The renderer should calculate the image’s destination footprint before JPX decode.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The renderer should calculate the image’s destination footprint before JPX decode.
            """
    }
}

record lege-ecosystem.work.reusable-bounded-scratch/1 : work {
    title "13. Reusable bounded scratch"
    state abandoned
    intent """
        `Jp2Decoder` should retain:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            `Jp2Decoder` should retain:
            """
    }
}

record lege-ecosystem.work.run-the-census-2nd-arg-max-files-omit-for-all/1 : work {
    title "2. Run the census (2nd arg = max files; omit for all 424). ~250s per ~34 files."
    state abandoned
    intent """
        DUMP="<some-writable-dir>"; mkdir -p "$DUMP"
        JPXDUMPDIR="$DUMP" cargo run -q -p pdf-cli --example jpxcensus -- /tmp/blankwin.txt 60
        # Dumped streams land in $DUMP/failjpx_<sig>.jp2 — opj_dump / opj_decompress them.
        ```
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
        excerpt """
            DUMP="<some-writable-dir>"; mkdir -p "$DUMP"
            JPXDUMPDIR="$DUMP" cargo run -q -p pdf-cli --example jpxcensus -- /tmp/blankwin.txt 60
            # Dumped streams land in $DUMP/failjpx_<sig>.jp2 — opj_dump / opj_decompress them.
            ```
            """
    }
}

record lege-ecosystem.work.specialize-the-common-code-block-style/1 : work {
    title "20. Specialize the common code-block style"
    state abandoned
    intent """
        The general Tier-1 loop carries runtime decisions for:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The general Tier-1 loop carries runtime decisions for:
            """
    }
}

record lege-ecosystem.work.thorough-ppocrv6-performance-and-output-preflight/1 : work {
    title "Audit thorough PP-OCRv6 throughput without sacrificing accuracy"
    state superseded
    scope [
        path "lege-document-ocr/scripts/benchmarks/**",
        path "lege-gpu/src/vision/**",
        path "lege-ocr/**",
        path "lege-process/**/tests/**",
        path "lege-process/core/main.rs",
        path "lege-process/pipeline/epub_pipeline.rs"
    ]
    intent """
        Profile the current thorough EPUB OCR path after the embedded PP-OCRv6 upgrade, retain V6 detector quality policy and all thorough recognition/recovery behavior, land only optimizations whose structured OCR output is unchanged or whose accuracy is independently demonstrated, and prevent an unwritable destination from wasting a full OCR run.
        """
    acceptance {
        check focused-tests {
            statement """
                Focused lege-ocr, lege-gpu vision, and EPUB pipeline tests pass.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.thorough-ppocrv6-focused-tests-20260822/1 ]
        }
        check generation-resolution-policy {
            statement """
                Tests retain detector limit_side 960 for PP-OCRv5 and 1280 for PP-OCRv6.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.thorough-ppocrv6-focused-tests-20260822/1 ]
        }
        check output-preflight {
            statement """
                An unwritable EPUB destination is rejected or safely redirected before page processing begins, with a focused regression test.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.epub-output-preflight-cli-20260822/1 ]
        }
        check quality-preserved {
            statement """
                Optimized thorough OCR emits identical structured text and geometry on the fixed benchmark inputs, or any intentional difference has ground-truth evidence that it is not a regression.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.thorough-ppocrv6-throughput-quality-20260822/1
            ]
        }
        check throughput-measured {
            statement """
                A representative release-mode benchmark attributes stage time and records the before/after throughput result; performance code is changed only when the result is faster without a quality loss.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.thorough-ppocrv6-throughput-quality-20260822/1
            ]
        }
    }
}

record lege-ecosystem.work.thorough-ppocrv6-performance-and-output-preflight/2 : work {
    title "Audit thorough PP-OCRv6 throughput without sacrificing accuracy"
    state completed
    scope [
        path "lege-document-ocr/scripts/benchmarks/**",
        path "lege-gpu/src/vision/**",
        path "lege-ocr/**",
        path "lege-process/core/main.rs",
        path "lege-process/pipeline/epub_pipeline.rs"
    ]
    intent """
        Profile the current thorough EPUB OCR path after the embedded PP-OCRv6 upgrade, retain V6 detector quality policy and all thorough recognition/recovery behavior, land only optimizations whose structured OCR output is unchanged or whose accuracy is independently demonstrated, and prevent an unwritable destination from wasting a full OCR run.
        """
    note """
        Scope corrected 2026-09-08: `lege-process/**/tests/**` matched no tracked file and never had —
        lege-process keeps no `tests/` directory, only `lege-process/scripts/test-paddle-ocr-gpu.sh` —
        so it was a scope that could not be verified against anything. Dropped rather than guessed at,
        since the other five paths already name what this work touched. Surfaced by AKR 0.5.0's
        extension of AKR-G023 to completed records; it had been invisible since the record was written.
        """
    acceptance {
        check focused-tests {
            statement """
                Focused lege-ocr, lege-gpu vision, and EPUB pipeline tests pass.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.thorough-ppocrv6-focused-tests-20260822/1 ]
        }
        check generation-resolution-policy {
            statement """
                Tests retain detector limit_side 960 for PP-OCRv5 and 1280 for PP-OCRv6.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.thorough-ppocrv6-focused-tests-20260822/1 ]
        }
        check output-preflight {
            statement """
                An unwritable EPUB destination is rejected or safely redirected before page processing begins, with a focused regression test.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.epub-output-preflight-cli-20260822/1 ]
        }
        check quality-preserved {
            statement """
                Optimized thorough OCR emits identical structured text and geometry on the fixed benchmark inputs, or any intentional difference has ground-truth evidence that it is not a regression.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.thorough-ppocrv6-throughput-quality-20260822/1
            ]
        }
        check throughput-measured {
            statement """
                A representative release-mode benchmark attributes stage time and records the before/after throughput result; performance code is changed only when the result is faster without a quality loss.
                """
            method command
            verified_by [
                @lege-ecosystem.evidence.thorough-ppocrv6-throughput-quality-20260822/1
            ]
        }
    }
    supersedes [ @lege-ecosystem.work.thorough-ppocrv6-performance-and-output-preflight/1 ]
}

record lege-ecosystem.work.tier-1-code-block-parallelism/1 : work {
    title "25. Tier-1 code-block parallelism"
    state abandoned
    intent """
        After reducing per-block allocation, decode independent code-blocks concurrently.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            After reducing per-block allocation, decode independent code-blocks concurrently.
            """
    }
}

record lege-ecosystem.work.tier-2-behavior/1 : work {
    title "7. Tier-2 behavior"
    state abandoned
    intent """
        A reduced-resolution decoder still needs to parse packet headers sufficiently to locate packet bodies and preserve tag-tree state, but it need not retain or Tier-1-decode high-resolution code-block data.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            A reduced-resolution decoder still needs to parse packet headers sufficiently to locate packet bodies and preserve tag-tree state, but it need not retain or Tier-1-decode high-resolution code-block data.
            """
    }
}

record lege-ecosystem.work.treat-9-7-as-the-first-arithmetic-kernel-target/1 : work {
    title "14. Treat 9/7 as the first arithmetic kernel target"
    state abandoned
    intent """
        The earlier flamegraph attributed about 34% of the original JPX page to irreversible 9/7 reconstruction. With renderer sampling reduced to approximately 35 ms, reconstruction is likely now the largest individual decoder stage.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            The earlier flamegraph attributed about 34% of the original JPX page to irreversible 9/7 reconstruction. With renderer sampling reduced to approximately 35 ms, reconstruction is likely now the largest individual decoder stage.
            """
    }
}

record lege-ecosystem.work.truetyping-vertical-placement/1 : work {
    title "Handoff: put truetyping's letters back on their own baseline"
    state completed
    scope [
        path "lege-codecs/jbig2enc-rust/src/encode/document/symbol/**",
        path "lege-pdf/write/src/text.rs",
        path "lege-process/core/pdf_artifact.rs",
        path "lege-process/encoding/glyphfont.rs"
    ]
    intent """
        Give every truetyping occurrence the vertical position it was scanned at, rather than its prototype's. The shapes may be shared — that is the point of the format — but the placement should not be, and JBIG2 symbol mode already shows what that looks like on the same clustering.
        """
    note """
        Raised by the user on 2026-09-03 after the substitution and glyph-space fixes (fff533a8) landed: most letters are now right, but some ride high within the word, and because the dictionary deduplicates so completely the wobble is the only difference left between two instances of a letter, which makes it read as letters out of place. Their comparison is plain JBIG2 symbol mode, not sym-unify, which does not show the effect.

        What to measure first, before changing anything. The suspected path is `process_analysis`: an occurrence's own bottom is discarded in favour of its prototype's `descent`, residuals within a pixel are snapped away and larger ones become position variants pinned to their first instance. But the share of the error that comes from the snap, from a prototype descent estimated on one page, from the Theil-Sen line baseline, and from component bottoms measured on broken serifs is *not known*. Instrument the difference between each occurrence's measured bottom and the y it is finally drawn at, and put the distribution in the glyph dump beside the fold sheet; the answer should say which of the four to fix and whether one pixel of the working raster is the whole story.

        What the fix probably looks like. `GlyphItem::rise` already carries a per-glyph text rise in thousandths of an em, and the writer already emits it — so an exact residual can be expressed without any new writer surface, and in font units rather than whole pixels. The reason it is not used today is deliberate and must be weighed rather than dropped: the variant machinery exists so every occurrence sits on its line's baseline with no rise, because poppler-based extractors break a line into separate runs at a rise change. Options in rough order of appeal: (1) keep the rise at zero and make the variant grid finer than a whole pixel, since the variants are already composites and cost little; (2) emit the exact residual as a rise and measure what it does to extraction (the recall script on Gobineau is the existing gauge); (3) revise a prototype's descent as its evidence grows instead of fixing it from the first page that saw it.

        Watch the file size while doing it: finer variants mean more glyph ids, and pass 9 already spends 4-5% more shapes on the matching gates.
        """
    claim the-corpus-to-judge-this-on-is-the-original-scan {
        text """
            The 836-page file pass 9 was fixed against, `historyofchinese00gern_0_1-1_processed_1784935875.pdf`, is a Lege output being reprocessed — a copy of a copy, chosen by mistake. The book to judge truetyping on is the original archive.org scan of the same title, which is greyscale and keeps its full resolution, and where the bilevel clamp does not apply.
            """
    }
    acceptance {
        check exact-occurrence-placement {
            statement """
                Truetyping preserves a one-pixel occurrence baseline difference and exact stacked-part offsets while sharing the underlying outlines and OCR mapping.
                """
            method command
            command "cargo test -p lege --lib encoding::glyphfont::tests"
            verified_by [ @lege-ecosystem.evidence.truetyping-exact-placement-regressions/1 ]
        }
        check lege-library-regression {
            statement """
                The complete lege library test suite remains green.
                """
            method command
            command "cargo test -p lege --lib"
            verified_by [ @lege-ecosystem.evidence.truetyping-exact-placement-lege-lib/1 ]
        }
    }
    derived_from [
        @lege-ecosystem.observation.truetyping-letters-ride-off-their-baseline,
        @lege-ecosystem.work.glyph-font-text-output
    ]
}

record lege-ecosystem.work.two-traps-do-not-fix-these-by-loosening-a-check/1 : work {
    title "5. Two traps — do NOT \"fix\" these by loosening a check"
    state abandoned
    intent """
        Both were tried this session and produce **silent mis-decodes**, which are
        worse than the current clean (B3-flagged) failures:
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
        excerpt """
            Both were tried this session and produce **silent mis-decodes**, which are
            worse than the current clean (B3-flagged) failures:
            """
    }
}

record lege-ecosystem.work.use-a-stripe-oriented-scratch-layout/1 : work {
    title "22. Use a stripe-oriented scratch layout"
    state abandoned
    intent """
        JPEG 2000 Tier-1 processes four-row stripes. The current coefficient vectors are row-major while the inner scan repeatedly visits the four vertical samples at each `x`.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            JPEG 2000 Tier-1 processes four-row stripes. The current coefficient vectors are row-major while the inner scan repeatedly visits the four vertical samples at each `x`.
            """
    }
}

record lege-ecosystem.work.vertical-lifting-layout/1 : work {
    title "16. Vertical lifting layout"
    state abandoned
    intent """
        Vertical DWT is likely less cache-friendly because samples are separated by the full image stride.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/decode-optimize.md"
        excerpt """
            Vertical DWT is likely less cache-friendly because samples are separated by the full image stride.
            """
    }
}

record lege-ecosystem.work.viewer-movement-and-reliability/1 : work {
    title "Complete lege-viewer stage 6 movement and start stage 7 reliability"
    state proposed
    scope [ path "lege-viewer/**" ]
    intent """
        Finish the one-scroll-model interaction vocabulary lege-viewer/STAGES.md calls stage 6 -- drag-to-pan, middle-click autoscroll, kinetic movement, configurable wheel scaling, page snapping, track and thumb refinement, and deterministic trace replay -- then take stage 7's two highest-value reliability items: the encrypted-document password flow and page-local failure handling.
        """
    note """
        Stage 6's exit gate is met for the scroll model: every movement source now applies a ScrollCommand to the one f64 model, and Page Up/Down semantics from Optimization A were not touched. Anchor-level trace replay is still not asserted.
        """
    claim a-broken-page-is-quarantined {
        text """
            A page that fails non-fatally is set aside after two failures, or one panic. Before this it was re-requested on every replan, refailed, and had each report answered with a full-canvas redraw, so one broken page burned a core forever on an idle document.
            """
    }
    claim autoscroll-must-be-visible {
        text """
            The autoscroll anchor is drawn on the canvas and the gesture ends on any wheel, any other click, focus loss or a DPI change. A mode that moves the document on its own must be visible, and a physical-pixel anchor stops meaning anything once the scale factor moves.
            """
    }
    claim encryption-is-classified-not-string-matched {
        text """
            An encrypted document is distinguished from a corrupt one by classifying the renderer's typed pdf_security::SecurityError, not by matching an error message. An unsupported security scheme stays a failure rather than a prompt, because no password the reader can type opens it.
            """
    }
    claim kinetic-is-a-preference-for-mice-and-mandatory-for-touch {
        text """
            Momentum on a released pointer drag is off by default because it reads as lag on a mouse; a touch flick always glides, because momentum's absence there reads as a broken surface. A long stall integrates at most one slow frame, so a suspended machine cannot teleport the document.
            """
    }
    claim one-recording-helper {
        text """
            Every movement source funnels through one recording helper rather than calling the scroll model directly, which is what makes an input trace complete by construction instead of complete by remembering to add each new call site.
            """
    }
    claim replay-uses-a-simulated-clock {
        text """
            Trace replay steps a simulated clock at the event loop's own 8 ms animation cadence rather than reading wall time, so a trace containing a kinetic fling reproduces the same final position on any machine.
            """
    }
}

record lege-ecosystem.work.what-was-already-fixed-do-not-redo/1 : work {
    title "What was already fixed (do not redo)"
    state abandoned
    intent """
        `ftyp` brand (`jpx ` major / `jp2 ` or `jpx ` compat), QCC per-component
        quantization, non-LRCP progression, TNsot-advisory multi-tile-part, raw J2K
        codestreams (SOC at offset 0), CMYK (EnumCS 12, 4-comp + MCT-on-first-3),
        `floor→ceil` decomposition bound, PLT/PLM/TLM length markers ignored, and
        `pclr`+`cmap` palette (added in parallel — verify it still holds if you touch
        color). See `pdf-renderer/DEFERRED.md` corpus item 1 for the full writeup.
        """
    note """
        Mis-imported ledger noise, abandoning the record and not the work. The 2026-08 bulk import of lege-codecs/jp2lam/decode-fix-plan/*.md promoted every markdown heading to its own work record and frequently attached an unrelated adjacent paragraph as the intent (work.add-jp2decodestats carried the 'do not start by optimizing MqDecoder' paragraph), so these records were misleading as well as redundant, and together they made ACTIVE-WORK.md unreadable. The real, current status of the jp2lam decoder optimisation is kept in lege-ecosystem.work.jp2lam-jpeg-2000-decoder-optimization-plan (Phase 0-3 landed, Phase 4 partial, Phase 5-6 open) and lege-ecosystem-perf.jp2.decode-optimization-handoff-2026-08-09; the residual decode gaps are in lege-ecosystem.work.handoff-remaining-jp2lam-jpeg-2000-decode-gaps, lege-ecosystem.work.zero-length-code-block-contribution-truncated and lege-ecosystem.work.pdf-renderer-follow-ups-outside-pure-codec. The source plan documents remain on disk under lege-codecs/jp2lam/decode-fix-plan/ and are the authority.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
        excerpt """
            `ftyp` brand (`jpx ` major / `jp2 ` or `jpx ` compat), QCC per-component
            quantization, non-LRCP progression, TNsot-advisory multi-tile-part, raw J2K
            codestreams (SOC at offset 0), CMYK (EnumCS 12, 4-comp + MCT-on-first-3),
            `floor→ceil` decomposition bound, PLT/PLM/TLM length markers ignored, and
            `pclr`+`cmap` palette (added in parallel — verify it still holds if you touch
            color). See `pdf-renderer/DEFERRED.md` corpus item 1 for the full writeup.
            """
    }
}

record lege-ecosystem.work.windows-document-ocr-winsafe-installer/1 : work {
    title "Package Lege Document OCR in a WinSafe Windows installer"
    state completed
    intent """
        Adapt the native WinSafe SinoRAG installer shell into an offline Lege Document OCR installer that embeds the complete TensorRT-first Windows payload in an LZMA2-compressed 7z archive, verifies installation readiness, and uninstalls only its registered product directory.
        """
    acceptance {
        check lzma2-payload {
            statement """
                The installer build creates and embeds the complete staged TensorRT OCR payload as a 7z archive using LZMA2.
                """
            method command
            command "powershell -File lege-document-ocr/scripts/build_windows_installer.ps1"
            verified_by [
                @lege-ecosystem.evidence.document-ocr-lzma2-installer-build-20260815/1
            ]
        }
        check offline-install {
            statement """
                An offline install extracts the CLI, TensorRT worker, models, cold engine builder, app-local runtime, licenses, and package manifest without SDK paths.
                """
            method command
            command "lege-document-ocr-installer.exe --install-dir <temp> --quiet --no-shortcuts"
            verified_by [ @lege-ecosystem.evidence.document-ocr-installer-smoke-20260815/1 ]
        }
        check safe-uninstall {
            statement """
                Uninstall is restricted to the registered Lege Document OCR install location and removes only product-created shortcuts, registry metadata, and install files.
                """
            method manual
            verified_by [
                @lege-ecosystem.evidence.document-ocr-safe-uninstall-smoke-20260815/1
            ]
        }
        check winsafe-shell {
            statement """
                A Lege-branded WinSafe installer and matching safe uninstaller build as workspace binaries.
                """
            method command
            command "cargo test -p lege-document-ocr-installer-winsafe && cargo build -p lege-document-ocr-installer-winsafe --release --bins"
            verified_by [ @lege-ecosystem.evidence.document-ocr-winsafe-crate-tests-20260815/1 ]
        }
    }
    derived_from [
        @lege-ecosystem.assessment.document-ocr-installer-readiness-20260815,
        @lege-ecosystem.work.package-primary-tensorrt-ocr-runtime
    ]
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli ]
}

record lege-ecosystem.work.windows-tensorrt-document-ocr-integration/1 : work {
    title "Integrate Windows TensorRT PP-OCRv6 with job-scoped WinOCR fallback"
    state completed
    scope [
        path "lege-document-ocr/README.md",
        path "lege-document-ocr/cli/src/main.rs",
        path "lege-document-ocr/crates/pipeline/src/lib.rs",
        path "lege-document-ocr/scripts/test_windows_tensorrt.ps1",
        path "lege-document-ocr/turboocr/**",
        path "lege-ocr/src/engine_tensorrt.rs",
        path "lege-ocr/src/lib.rs"
    ]
    intent """
        Integrate the native Windows PP-OCRv6 TensorRT worker into lege-document-ocr, selecting WinOCR only when GPU startup preflight is unavailable or fails and never changing OCR engines after a job starts.
        """
    claim backend-selection {
        text """
            On Windows, auto performs a real TensorRT detector/recognizer inference preflight before processing and selects either TensorRT or WinOCR for the complete batch.
            """
    }
    claim explicit-gpu-test {
        text """
            The tensorrt-paddle selection is fail-closed and the Windows smoke script exercises both the native probe and integrated PDF path.
            """
    }
    claim no-mid-job-switch {
        text """
            A TensorRT failure after processing starts fails the job and never retries its pages with WinOCR.
            """
    }
    acceptance {
        check gpu-integration {
            statement """
                The explicit TensorRT backend completes the restored 10-page PDF in a release build and records TensorRT model provenance.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.windows-tensorrt-integrated-release-pdf/1 ]
        }
        check operator-test {
            statement """
                The reusable Windows TensorRT smoke script passes its native probe and integrated PDF test.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.windows-tensorrt-smoke-script/1 ]
        }
        check regression-tests {
            statement """
                The OCR engine, document pipeline, and CLI test suites pass with formatting checks.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.windows-tensorrt-ocr-regression-tests/1 ]
        }
        check startup-routing {
            statement """
                Auto selects TensorRT when its real-inference preflight succeeds and selects WinOCR for the whole batch when the runtime is unavailable.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.windows-ocr-startup-routing/1 ]
        }
    }
    derived_from [
        @lege-ecosystem.work.ocr-backend-scheduler,
        @lege-ecosystem.work.windows-tensorrt-ocr-runtime
    ]
}

record lege-ecosystem.work.windows-tensorrt-ocr-runtime/1 : work {
    title "Port TurboOCR TensorRT text runtime to Windows"
    state completed
    scope [ path "lege-document-ocr/turboocr/**" ]
    intent """
        Create a native Windows/MSVC CUDA and TensorRT runtime seam, execute PP-OCRv6 detection and recognition on restored benchmark pages, and measure speed, memory, and text-layer accuracy before adapting the runtime for OCDNet/OCRNet.
        """
    acceptance {
        check relative-benchmark {
            statement """
                A restored multi-page sample has recorded TensorRT throughput, memory, and text-layer accuracy proxy results comparable with the prior Windows OCR and Paddle/WGPU runs.
                """
            method observation
            verified_by [
                @lege-ecosystem.evidence.windows-tensorrt-relative-benchmark-20260814/1
            ]
        }
        check text-pipeline-smoke {
            statement """
                The lean PP-OCRv6 detector and recognizer build with Windows CUDA and produce OCR text from a rendered benchmark page.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.windows-tensorrt-text-smoke-20260814/1 ]
        }
        check windows-runbook {
            statement """
                A Windows runbook documents prerequisites, build/model commands, port boundaries, and measured results.
                """
            method manual
            verified_by [ @lege-ecosystem.evidence.windows-tensorrt-runbook-20260814/1 ]
        }
        check windows-runtime-build {
            statement """
                The isolated TensorRT runtime configures and builds with MSVC on Windows and its CUDA/TensorRT device probe passes.
                """
            method command
            verified_by [ @lege-ecosystem.evidence.windows-tensorrt-runtime-build-20260814/1 ]
        }
    }
    derived_from [ @lege-ecosystem.work.ocr-backend-scheduler/4 ]
    part_of [ @lege-ecosystem.milestone.business-pdf-ocr-cli ]
}

record lege-ecosystem.work.zero-length-code-block-contribution-truncated/1 : work {
    title "3. Zero-length code-block contribution (truncated streams) — ~6 files"
    state proposed
    intent """
        - **Error:** `non-empty code-block contribution has zero byte length`
        - **Repro:** was seen on `Stalin…` (that hit is an `.epub`, not a PDF — find a
          PDF trigger by re-running the census with `JPXDUMPDIR` set and grabbing the
          dumped `failjpx_…zerobytelength….jp2`).
        - **Reject site:** `src/decode/t2.rs` ~line 481-485, in the packet-header
          contribution reader: a code-block signals inclusion but decodes a body length
          of 0, which the code treats as malformed.
        - **Diagnosis:** these are **truncated** streams (the file is cut off; the
          packet header promises data the body doesn't contain). PDFium/OpenJPEG salvage
          the partial image (they render what decoded). `jp2lam` errors → blank.
        - **Recommended approach (careful — correctness risk):** treat a truncated tail
          as *end of data*, not a hard error: stop Tier-2 at the first packet that runs
          past the available bytes, keep the coefficients decoded so far, and let
          reconstruction produce the partial image (this mirrors how `pdf-structure`
          already salvages truncated Flate content streams). The catch is that the
          reconstruct path currently expects a complete packet sequence (`finish()` at
          t2.rs ~line 225 errors on `ended early`). You'll need reconstruct to tolerate
          missing contributions (zero-fill those code-blocks). **Verify the partial
          output matches OpenJPEG's partial output**, not just "it doesn't error" — a
          partial decode that diverges from OpenJPEG is a silent quality regression.
        - **Priority:** lower than §1 — truncation is inherently lossy and rare.
        """
    source {
        kind legacy
        path "lege-codecs/jp2lam/decode-fix-plan/HANDOFF-remaining-jpx-decode.md"
        excerpt """
            - **Error:** `non-empty code-block contribution has zero byte length`
            - **Repro:** was seen on `Stalin…` (that hit is an `.epub`, not a PDF — find a
              PDF trigger by re-running the census with `JPXDUMPDIR` set and grabbing the
              dumped `failjpx_…zerobytelength….jp2`).
            - **Reject site:** `src/decode/t2.rs` ~line 481-485, in the packet-header
              contribution reader: a code-block signals inclusion but decodes a body length
              of 0, which the code treats as malformed.
            - **Diagnosis:** these are **truncated** streams (the file is cut off; the
              packet header promises data the body doesn't contain). PDFium/OpenJPEG salvage
              the partial image (they render what decoded). `jp2lam` errors → blank.
            - **Recommended approach (careful — correctness risk):** treat a truncated tail
              as *end of data*, not a hard error: stop Tier-2 at the first packet that runs
              past the available bytes, keep the coefficients decoded so far, and let
              reconstruction produce the partial image (this mirrors how `pdf-structure`
              already salvages truncated Flate content streams). The catch is that the
              reconstruct path currently expects a complete packet sequence (`finish()` at
              t2.rs ~line 225 errors on `ended early`). You'll need reconstruct to tolerate
              missing contributions (zero-fill those code-blocks). **Verify the partial
              output matches OpenJPEG's partial output**, not just "it doesn't error" — a
              partial decode that diverges from OpenJPEG is a silent quality regression.
            - **Priority:** lower than §1 — truncation is inherently lossy and rare.
            """
    }
}
