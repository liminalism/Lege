//! `lege-docwrite`: the page-native book editor.
//!
//! Planned: a pixelkit host showing the book as stacked pages, a Book Map
//! sidebar, a minimal toolbar, exact page/spread navigation with snap at the
//! end of a scroll gesture, and keypress-to-present latency instrumentation.
//!
//! This binary is a scaffold; see the project ledger (`.akr/`) for scope.

fn main() {
    println!("lege-docwrite: scaffold only; nothing to run yet");
}
