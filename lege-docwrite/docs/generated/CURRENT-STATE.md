<!-- GENERATED BY AKR — DO NOT EDIT
     source-graph: sha256:d786454dde453e61fcf5392d5c01cc5875f8376f615f116ee1d320b355575124
     tool: akr 0.5.1
-->

# Current state — lege-docwrite

What the project believes right now: live terms, constraints, policies and requirements, and what has been found or assessed. Decisions have their own view, `DECISION-HISTORY.md`.

## Terms

_(none)_

## Constraints

_(none)_

## Policies

_(none)_

## Requirements

### PageUp/PageDown move exactly one physical page or spread

`proposed` · `@docwrite.req.exact-page-navigation/1` · scope `all` · topic `navigation`

PageDown always lands on the top of the next physical page and PageUp on the previous;
in spread mode they move one spread. Continuous mouse or trackpad scrolling stays
continuous during the gesture and settles magnetically onto the nearest page (or spread)
when the gesture ends. Jumps by page number, chapter, outline entry and search result are
deterministic.

### Typing never waits on work unrelated to the edited paragraph

`proposed` · `@docwrite.req.typing-latency/1` · scope `all` · topic `typing-latency`

The hot path is key event → edit transaction → text mutation → reshape the affected
paragraph → incremental repagination to convergence → damage → present. Nothing else
runs on it: spell checking, image decoding, citation processing, index rebuilding,
search indexing, autosave serialization and export are asynchronous. For ordinary
typing in a 500-page book the application spends only a few milliseconds before handing
the frame to the presenter, and keypress-to-present latency is a regression gate.

## Observations

### M1–M5 were closed on synthetic test paths, not the editor

`verified` · `@docwrite.observation.milestones-closed-on-synthetic-paths/1`

At 8cc8d8f the ledger marked M1–M5 completed, but their evidence exercised
synthetic paths rather than the editor a writer uses. The window's Editor
cloned the Book and ran from_book on every paint and paged over a hardcoded
eight pages; the latency gate typed through Document::edit_page on a
one-line-per-page synthetic book. export_pdf put eight blocks per page, each
paragraph as one run in the invisible OCR text layer, so the PDF was
searchable but rendered blank. The one-line-per-page fast path put every
short paragraph on its own page whenever no chapter started recto. Chapter
titles never reached their openers or the Markdown/IDML exports, hyphenated
breaks drew no hyphen, and the binary could not open, save or export a book.

> **Stale** — `watches "lege-docwrite/crates/docwrite-app/src/**"` was matched by `06ef587e`, which touched `lege-docwrite/crates/docwrite-app/src/commands.rs`. See [REVIEW-REQUIRED.md](REVIEW-REQUIRED.md#m1m5-were-closed-on-synthetic-test-paths-not-the-editor).

### Nothing in Lege-ecosystem shapes, breaks or hyphenates book text

`verified` · `@docwrite.observation.no-shaping-in-workspace/3`

No ecosystem crate depends on harfbuzz/rustybuzz/harfrust/swash directly; shaping is only
reachable through cosmic-text in lege-viewer's chrome. pdf-font (skrifa 0.44) parses
TrueType, CFF, Type1 and CID programs and exposes outlines, gid_for_char and advances but
no GSUB/GPOS. There is no unicode-linebreak, unicode-segmentation, hyphenation or
Knuth-Plass code. ropey is declared in the root workspace but no Lege crate uses it.

**supersedes** `@docwrite.observation.no-shaping-in-workspace/2`

### Lege's text extraction already supports page-anchored source passages

`verified` · `@docwrite.observation.pdf-text-supports-source-passages/2`

pdf_text::TextPage exposes per-character origin, boxes and matrices, words(), rects() and
text_in_rect() over UTF-16 ranges; lege_pdf_read::positioned_words returns word boxes per
page; lege-viewer's text/selection.rs implements hit-testing and word/line/multi-page
selection. A Source Note can therefore record document, page, UTF-16 range and rectangles
and reopen the source at exactly that passage.

**supersedes** `@docwrite.observation.pdf-text-supports-source-passages/1`

### lege-pdf-write lacks what typeset-book PDF output needs

`verified` · `@docwrite.observation.pdf-write-gaps/3`

lege-pdf-write is still image-oriented and append-only: DocumentWriter needs the page count up front. Content operators are q, Q, g, cm, Do and the text set BT ET Tr Tc Tw Tz Tf Tm Tj TJ Ts; there are no path operators and fill is gray only. Fonts are Type0/Identity-H CIDFontType2 with FontFile2 only, no CFF/FontFile3. The writer embeds caller-supplied bytes and does not subset. ToUnicode is None, Identity, or a caller-supplied CMap. Several glyph fonts can be registered on the document. Image XObjects cover DCT, JPX, JBIG2, CCITT and indexed DeviceRGB; there is no Flate RGB image and no alpha channel. Outline bookmarks and metadata remain. The catalog can now carry /Lang, a /StructTreeRoot of element names, and /PageLabels. Link annotations and named destinations are still absent.

**supersedes** `@docwrite.observation.pdf-write-gaps/2`

> **Stale** — `watches "lege-pdf/write/src/**"` was matched by `352a95d3`, which touched `lege-pdf/write/src/artifact.rs`. See [REVIEW-REQUIRED.md](REVIEW-REQUIRED.md#lege-pdf-write-lacks-what-typeset-book-pdf-output-needs).

### pixelkit-shell needs presenter injection, IME, scroll phases and fuller key input

`verified` · `@docwrite.observation.pixelkit-shell-gaps/2`

pixelkit-shell hardcodes SoftbufferPresenter in resumed (the Presenter trait already
exists and has a Gpu backend variant); never calls set_ime_allowed or
set_ime_cursor_area, so IME events do not arrive; drops scroll TouchPhase and
pinch/magnify; keeps only the first char of Character input, emits no key-up and maps
few function keys; FrameTimer records paint and present but not input-to-present, and
tick is recorded as zero. Rendering repaints the full frame with no damage rects. The
single-line TextFieldState has no word motion, shift-extend, clipboard or undo.

**supersedes** `@docwrite.observation.pixelkit-shell-gaps/1`

### pixelkit-text cannot typeset books

`verified` · `@docwrite.observation.pixelkit-text-limits/2`

pixelkit-text parses only TrueType glyf outlines (no CFF, variable fonts or
collections), applies a fixed feature set (GSUB ccmp/liga, GPOS kern/mark/mkmk) with no
way to request smcp/onum/etc., skips GSUB type 5/7 and GPOS types 1/3/5/7/8/9, ignores
the ScriptList, has no bidi or UAX #14 line breaking, and caches rasterized whole strings
rather than glyphs.

**supersedes** `@docwrite.observation.pixelkit-text-limits/1`

### lege-viewer pages by line continuity and snaps only on command

`verified` · `@docwrite.observation.viewer-paging-is-line-continuity/2`

lege-viewer's PageUp/PageDown is line-continuity paging: paging_target makes the last
fully visible line the new top line, with notional-line and 8%-overlap geometric
fallbacks (lege-viewer/src/scroll/paging.rs). Nearest-page snap is an explicit command
(nearest_page_boundary, bound to S) and never happens automatically. Reusable, not
PDF-coupled: scroll/anchor.rs, frame.rs, damage.rs, diagnostics.rs, trace.rs, scene.rs,
present/*. The conductor/tile pipeline and the Pdf-variant artifact enums in
document/engine.rs assume read-only content.

**supersedes** `@docwrite.observation.viewer-paging-is-line-continuity/1`

## Assessments

_(none)_

## Evidence

### Typing does not create the bundle. Autosave writes it. Restoring the named snapshot drops the keystrokes that came after the snapshot.

`verified` · `@docwrite.evidence.autosave-off-path/1`

Typing does not create the bundle. Autosave writes it. Restoring the named snapshot drops the keystrokes that came after the snapshot.

**Verifies**

- `completed` `@docwrite.work.m2-persistence/1` — check `autosave-off-path`

### A named snapshot reopens the earlier manuscript, Body size, template opener, and a non-collapsed selection. The live bundle keeps the later edit. Typing does not write the bundle until autosave.

`verified` · `@docwrite.evidence.autosave-snapshot/1`

A named snapshot reopens the earlier manuscript, Body size, template opener, and a non-collapsed selection. The live bundle keeps the later edit. Typing does not write the bundle until autosave.

**Verifies**

- `completed` `@docwrite.work.m2-persistence/1` — check `snapshot-reopen`

### Dragging a chapter row reorders that chapter inside its part. Collapsing the body hides its chapters. Front matter and back matter stay listed, and a body drag leaves the preface in front matter.

`verified` · `@docwrite.evidence.book-map-drag/1`

Dragging a chapter row reorders that chapter inside its part. Collapsing the body hides its chapters. Front matter and back matter stay listed, and a body drag leaves the preface in front matter.

**Verifies**

- `completed` `@docwrite.work.m2-book-map/1` — check `drag-reorder`

### NextPage chapters start a page; each chapter uses its template's master

`verified` · `@docwrite.evidence.chapter-rules-and-masters/1`

A NextPage chapter opens the very next page; a chapter on an Appendix template
takes its master's margins (inset per facing side), measure and running-head
rule, while chapter one keeps its own. NextRecto still blanks a verso, with a
pending footnote carried across it.

**Verifies**

- `proposed` `@docwrite.milestone.m6-writer-ready/1` — check `chapter-rules`

### The window's commands work: clipboard, undo, line motion across pages, click, save, autosave, export, CLI export

`verified` · `@docwrite.evidence.editor-commands/1`

Four tests cover cut/copy/paste with undo/redo, Up/Down by typeset line across a
page turn, click-to-caret, and open_or_create + save_now + background autosave +
PDF/Markdown export + the headless 'lege-docwrite export' subcommand. The window
was also launched on macOS against a demo bundle and screenshot: the opener shows
its chapter title, the Book Map shows titles, and the page fills the view.

### Typing through Editor into a 507-page book: one paragraph shaped, at most three pages laid out, p50 4.4 ms, max 4.9 ms (release)

`verified` · `@docwrite.evidence.editor-latency-release/1`

typing_into_the_editor_stays_inside_the_budget types at the start, middle and end
of a 507-page book through Editor::type_text + Editor::paint (1100x800). Every
keystroke shaped exactly one paragraph and laid out at most three pages; wall
clock p50 4.43 ms, max 4.95 ms against the 8 ms budget; opening the book 67 ms.
Debug builds assert the work counts only.

### Typing through Editor into a 550-page book: p50 1.94 ms, max 2.21 ms (release)

`verified` · `@docwrite.evidence.editor-latency-release-2026-09-24/1`

With the font family, justification and styled paragraphs in place, typing at the
start, middle and end of a 550-page book shaped one paragraph and laid out at most
three pages per key; keypress-to-present p50 1.94 ms, max 2.21 ms against 8 ms;
opening 42 ms.

### Recto inset 54, verso inset 20. Blank verso page 2 keeps folio 2 and running head Chapter 1. With a 49-character footnote still pending, page 2 stays blank and Two. opens on page 3; the note reassembles after edit_page.

`verified` · `@docwrite.evidence.facing-recto/1`

Recto inset 54, verso inset 20. Blank verso page 2 keeps folio 2 and running head Chapter 1. With a 49-character footnote still pending, page 2 stays blank and Two. opens on page 3; the note reassembles after edit_page.

**Verifies**

- `completed` `@docwrite.work.m2-styles-masters-templates/1` — check `facing-recto`

### With a 49-character footnote still pending, page 2 stays blank and Two. opens on page 3. The note slices reassemble after edit_page.

`verified` · `@docwrite.evidence.head-blank-verso/1`

With a 49-character footnote still pending, page 2 stays blank and Two. opens on page 3. The note slices reassemble after edit_page.

**Verifies**

- `completed` `@docwrite.milestone.m4-research-apparatus/2` — check `footnotes-paginate`

### Edit tests round-trip insert, delete, and undo. Ten chapters follow the Lesson opener and reopen restores Body at 14pt and the caret.

`verified` · `@docwrite.evidence.head-edits/1`

Edit tests round-trip insert, delete, and undo. Ten chapters follow the Lesson opener and reopen restores Body at 14pt and the caret.

**Verifies**

- `completed` `@docwrite.milestone.m1-editor-spike/2` — check `edit-basics`

### Markdown keeps the title and the link and drops page geometry. The PDF citation resolves to page 0 with highlight rects [[72.0, -90.0, 198.0, -72.0]] for Chapter. Tagged PDF has /Lang and /StructTreeRoot. Subset font is 9184 bytes against a 512672-byte face. IDML contains styles, parent pages, a threaded story, an anchored image, and footnotes.

`verified` · `@docwrite.evidence.head-exports/1`

Markdown keeps the title and the link and drops page geometry. The PDF citation resolves to page 0 with highlight rects [[72.0, -90.0, 198.0, -72.0]] for Chapter. Tagged PDF has /Lang and /StructTreeRoot. Subset font is 9184 bytes against a 512672-byte face. IDML contains styles, parent pages, a threaded story, an anchored image, and footnotes.

**Verifies**

- `completed` `@docwrite.milestone.m2-book-core/2` — check `exports`

### A long footnote splits into 6 pieces and reassembles on the reference page.

`verified` · `@docwrite.evidence.head-footnotes/1`

A long footnote splits into 6 pieces and reassembles on the reference page.

**Verifies**

- `completed` `@docwrite.milestone.m4-research-apparatus/2` — check `footnotes-paginate`

### Page 147 of 400 laid out [147]. Every rebuilt page is below 149 and page 400 is unchanged.

`verified` · `@docwrite.evidence.head-incremental/1`

Page 147 of 400 laid out [147]. Every rebuilt page is below 149 and page 400 is unchanged.

**Verifies**

- `completed` `@docwrite.milestone.m1-editor-spike/2` — check `convergent-repagination`

### A 46-frame typing trace on a 500-page document stays inside the 8ms keypress budget.

`verified` · `@docwrite.evidence.head-latency/1`

A 46-frame typing trace on a 500-page document stays inside the 8ms keypress budget.

**Verifies**

- `completed` `@docwrite.milestone.m1-editor-spike/2` — check `five-hundred-pages`

### Page Down and Page Up land on page tops, a spread gesture snaps, and paint lays out at least one page of the manuscript.

`verified` · `@docwrite.evidence.head-navigation/1`

Page Down and Page Up land on page tops, a spread gesture snaps, and paint lays out at least one page of the manuscript.

**Verifies**

- `completed` `@docwrite.milestone.m1-editor-spike/2` — check `exact-paging`

### Ten chapters follow the Lesson opener. Reopen restores Body at 14pt, opener Lesson, and the caret.

`verified` · `@docwrite.evidence.head-persistence/1`

Ten chapters follow the Lesson opener. Reopen restores Body at 14pt, opener Lesson, and the caret.

**Verifies**

- `completed` `@docwrite.milestone.m2-book-core/2` — check `reorder-and-reopen`
- `completed` `@docwrite.milestone.m2-book-core/2` — check `ten-chapter-book`

### Preflight reports missing font Noto Sans, unembeddable Body, low-resolution image plate, and broken reference missing-figure.

`verified` · `@docwrite.evidence.head-preflight/1`

Preflight reports missing font Noto Sans, unembeddable Body, low-resolution image plate, and broken reference missing-figure.

**Verifies**

- `completed` `@docwrite.milestone.m5-interchange/2` — check `preflight`

### A body restyle at 18pt matches a fresh layout across 12 pages.

`verified` · `@docwrite.evidence.head-restyle/1`

A body restyle at 18pt matches a fresh layout across 12 pages.

**Verifies**

- `completed` `@docwrite.milestone.m2-book-core/2` — check `global-style-change`

### Keep and widow violations are 0. Hyphenation, a 24em drop cap, OpenType small caps and old-style figures, and image-with-caption are present.

`verified` · `@docwrite.evidence.head-rules/1`

Keep and widow violations are 0. Hyphenation, a 24em drop cap, OpenType small caps and old-style figures, and image-with-caption are present.

**Verifies**

- `completed` `@docwrite.milestone.m3-prose-typography/2` — check `paragraph-rules`

### A passage extracted from the exported PDF is cited and resolves to page 0, highlight rects [[72.0, -90.0, 198.0, -72.0]], passage Chapter.

`verified` · `@docwrite.evidence.head-source/1`

A passage extracted from the exported PDF is cited and resolves to page 0, highlight rects [[72.0, -90.0, 198.0, -72.0]], passage Chapter.

**Verifies**

- `completed` `@docwrite.milestone.m4-research-apparatus/2` — check `source-roundtrip`

### The contents move chapter Two from page 3 to page 5 after a cross-page insertion. One stays on page 1.

`verified` · `@docwrite.evidence.head-toc/1`

The contents move chapter Two from page 3 to page 5 after a cross-page insertion. One stays on page 1.

**Verifies**

- `completed` `@docwrite.milestone.m3-prose-typography/2` — check `generated-toc`

### Incremental layout of real model edits matches a fresh layout page for page

`verified` · `@docwrite.evidence.incremental-matches-fresh/1`

Typing, paragraph split and merge, a multi-page paste, undo/redo, a span delete,
emptying the last paragraph, chapter reorders and a chapter rename, applied
through update_from_book, each equal from_book of the same book (blank versos,
folios, running heads, lines, notes), with and without recto chapter starts.

### Insert, delete, selection, word motion, cut/copy/paste and undo/redo round-trip across paragraphs and chapters.

`verified` · `@docwrite.evidence.m1-edit-basics/1`

Insert, delete, selection, word motion, cut/copy/paste and undo/redo round-trip across paragraphs and chapters.

**Verifies**

- `superseded` `@docwrite.milestone.m1-editor-spike/1` — check `edit-basics`

### An edit on page 147 of 400 lays out no page from 149 through 400.

`verified` · `@docwrite.evidence.m1-incremental/1`

An edit on page 147 of 400 lays out no page from 149 through 400.

**Verifies**

- `superseded` `@docwrite.milestone.m1-editor-spike/1` — check `convergent-repagination`

### A 500-page document opens, pages and types inside the 8ms keypress budget.

`verified` · `@docwrite.evidence.m1-latency/1`

A 500-page document opens, pages and types inside the 8ms keypress budget.

**Verifies**

- `superseded` `@docwrite.milestone.m1-editor-spike/1` — check `five-hundred-pages`

### PageDown and PageUp land on page tops; spread mode moves by spread; gesture end snaps.

`verified` · `@docwrite.evidence.m1-navigation/1`

PageDown and PageUp land on page tops; spread mode moves by spread; gesture end snaps.

**Verifies**

- `superseded` `@docwrite.milestone.m1-editor-spike/1` — check `exact-paging`

### PDF text extracts, chapter bookmarks and a subset font are present; Markdown keeps headings, emphasis, links and drops geometry.

`verified` · `@docwrite.evidence.m2-exports/1`

PDF text extracts, chapter bookmarks and a subset font are present; Markdown keeps headings, emphasis, links and drops geometry.

**Verifies**

- `superseded` `@docwrite.milestone.m2-book-core/1` — check `exports`

### Reorder changes chapter order; reopen restores manuscript, Body size, template opener and caret. One template edit changes all ten openers.

`verified` · `@docwrite.evidence.m2-persistence/1`

Reorder changes chapter order; reopen restores manuscript, Body size, template opener and caret. One template edit changes all ten openers.

**Verifies**

- `superseded` `@docwrite.milestone.m2-book-core/1` — check `reorder-and-reopen`
- `superseded` `@docwrite.milestone.m2-book-core/1` — check `ten-chapter-book`

### Restyling Body matches a fresh layout of the same paragraphs.

`verified` · `@docwrite.evidence.m2-restyle/1`

Restyling Body matches a fresh layout of the same paragraphs.

**Verifies**

- `superseded` `@docwrite.milestone.m2-book-core/1` — check `global-style-change`

### Keep-with-next holds; small caps and old-style figures change glyph ids; drop caps, quotes, epigraphs, images and matter are in the corpus.

`verified` · `@docwrite.evidence.m3-rules/1`

Keep-with-next holds; small caps and old-style figures change glyph ids; drop caps, quotes, epigraphs, images and matter are in the corpus.

**Verifies**

- `superseded` `@docwrite.milestone.m3-prose-typography/1` — check `paragraph-rules`

### Inserting paragraphs before a chapter moves its contents page number.

`verified` · `@docwrite.evidence.m3-toc/1`

Inserting paragraphs before a chapter moves its contents page number.

**Verifies**

- `superseded` `@docwrite.milestone.m3-prose-typography/1` — check `generated-toc`

### Footnotes stay on the reference page under an earlier edit, and a long note splits across later pages and reassembles.

`verified` · `@docwrite.evidence.m4-footnotes/1`

Footnotes stay on the reference page under an earlier edit, and a long note splits across later pages and reassembles.

**Verifies**

- `superseded` `@docwrite.milestone.m4-research-apparatus/1` — check `footnotes-paginate`

### A word captured from the exported PDF, with its page and rectangle, is cited and resolves to that same page and rectangle.

`verified` · `@docwrite.evidence.m4-source/1`

A word captured from the exported PDF, with its page and rectangle, is cited and resolves to that same page and rectangle.

**Verifies**

- `superseded` `@docwrite.milestone.m4-research-apparatus/1` — check `source-roundtrip`

### Preflight reports a missing face, an unembeddable font, a low-resolution image and a broken link.

`verified` · `@docwrite.evidence.m5-preflight/1`

Preflight reports a missing face, an unembeddable font, a low-resolution image and a broken link.

### On f19325d, preflight reports a missing face, an unembeddable font, a 72 dpi image, and a broken link.

`verified` · `@docwrite.evidence.m5-preflight-current/1`

On f19325d, preflight reports a missing face, an unembeddable font, a 72 dpi image, and a broken link.

**Verifies**

- `superseded` `@docwrite.milestone.m5-interchange/1` — check `preflight`

### Footnotes are measured into the page and split by lines

`verified` · `@docwrite.evidence.measured-footnotes/1`

A footnote takes its measured height from its reference page's text block (fewer
body lines than the same page without it), sits at the foot below a rule at
footnote size, and a long note continues on later pages line by line with every
word set once, in order; it stays on its reference page after an earlier edit.

**Verifies**

- `proposed` `@docwrite.milestone.m6-writer-ready/1` — check `measured-footnotes`

### Exported PDF renders the typeset pages with ink, one PDF page per layout page

`verified` · `@docwrite.evidence.pdf-renders-typeset-pages/1`

pdf_pages_are_the_typeset_pages_and_visibly_inked renders page 1 of the export
through lege-pdf-read (over 2000 dark pixels), checks the PDF page count equals
the layout's, that page 1's text starts with the first typeset line in reading
order, and that the chapter bookmark exists.

### Extending the caret paints selection pixels behind the glyphs. A collapsed caret paints none.

`verified` · `@docwrite.evidence.selection-highlight/1`

Extending the caret paints selection pixels behind the glyphs. A collapsed caret paints none.

**Verifies**

- `completed` `@docwrite.work.m1-page-canvas-navigation/1` — check `selection-highlight`

### Changing the chapter template opener from Chapter to Lesson changed all ten chapter openers, and reopening restored the template.

`verified` · `@docwrite.evidence.template-propagates/1`

Changing the chapter template opener from Chapter to Lesson changed all ten chapter openers, and reopening restored the template.

**Verifies**

- `completed` `@docwrite.work.m2-styles-masters-templates/1` — check `template-propagates`

### A 46-frame typing trace on a 500-page document stayed inside 8ms. Each keypress laid out only the edited page and the next, and the typed page text starts with k.

`verified` · `@docwrite.evidence.typing-trace/1`

A 46-frame typing trace on a 500-page document stayed inside 8ms. Each keypress laid out only the edited page and the next, and the typed page text starts with k.

**Verifies**

- `completed` `@docwrite.work.m1-latency-gate/1` — check `typing-trace`

### Editor commands, toolbar, sidebar, full screen, spreads, research pane, find/replace and typography pass their tests

`verified` · `@docwrite.evidence.window-features-2026-09-24/1`

Covers: resizable Book Map folding to a tab; WordPerfect-style full screen with a
status line; spread view; toolbar marks, styles, sizes, chapter/rename/note
prompts; find, find-again, replace-all (one undo); smart quotes/dashes/ellipses;
research pane capture, quote, cite and citation follow-through; lossless format-2
bundles with format-1 migration and crash-safe swap; bold/italic families,
justification, indents, drop caps, synthesized small caps; contents, Notes,
Bibliography and Index sections. Views were also checked in offscreen PNG
snapshots. Not covered: an interactive session in the window by a person.
