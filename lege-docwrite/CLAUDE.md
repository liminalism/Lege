# CLAUDE.md

See `AGENTS.md` for the agent protocol and `README.md` for the product.
Plans, decisions and milestones live in the AKR ledger (`.akr/`), not in
Markdown: start with `akr context` / `knowledge.context`, and never hand-edit
`.akr/` or `docs/generated/`.
